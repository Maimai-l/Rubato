# EXPERIMENT_H1 —— 裁剪阈值对照实验(平台期悬案的判决数据)

> 读者:执行端。整卡只有一件事:备份 → 换 `--clip-norm 25` 续训 ≥500 步 → 按清单贴回。
> 判决由规划端按本卡预登记的判据做,你只负责跑和贴,不需要解读。

## 0. 背景(每个数字有出处)

- 全量训练 step 4000→7600 接近平台期:A2S 200 窗均值 ≈3.21,近段速率 ≈ **-0.06 / 350 步**
  【出处:用户粘贴日志 2026-07-14,原文存 HANDOFF.md §4】。
- 裁剪前梯度范数真实值 gn ≈ 21.6~37.9(50 步均值 23.5~27.8),而 clip 阈值 = 1.0
  【出处:同上,原文照录于 HANDOFF.md §4】。
- **假设 H1**:阈值 1.0 把 ~25 的梯度每步等比压回 → 有效步长只剩 ~4% → 平台期。
- **规划端预登记先验(2026-07-14,判决前写下,防马后炮)**:优化器是 AdamW
  (train.py build_optimizer),其更新量 m̂/√v̂ 对【恒定比例】的梯度缩放近似不变
  (分子分母同比缩放相消)——理论上 H1 在 AdamW 下偏弱,**预期本实验更可能判 H1 不成立**。
  但按项目铁律只认数字:是否成立由本实验的贴回数据判决,不由理论代替。
  实验无论哪个结果都不白跑:新日志列 enc=/dec=(分组裁剪前范数)是 H2 的直接观测,白拿。

## 1. 执行步骤

```bat
:: (0) 拉最新(拿到本卡 + 新日志代码;日志行会多出 enc=/dec= 两列)
git pull --rebase --autostash

:: (1) 备份当前快照(实验保险,必做;文件名里的步数以你看到的恢复行实际步数为准,
::     名字不完全一致不要紧,备份文件存在即可)
copy /Y D:\vscode_projects\ee_download\outputs\ckpt\last.pt D:\vscode_projects\ee_download\outputs\ckpt\last_clip1_pre_h1.pt

:: (2) 启动实验(其余参数全默认,与此前全量续训完全一致;自动从 last.pt 恢复)
python scripts/build_dataset.py --clip-norm 25
```

- 跑到 **恢复点 +500 步以上**(例:`续训:恢复 step=7600` → 至少跑到 8100)。
- 途中会撞上 step 8000 的 eval,属正常,约 20 分钟,不要打断。
- 跑完(或触发下面的止损)即可 Ctrl+C 停,按 §3 贴回。

## 2. 跑的时候盯什么(止损规则,提前声明)

- 前 ~100 步是 AdamW 二阶矩对新阈值重新平衡的过渡期,loss 出现**小幅**尖峰属预期,不动。
- 【硬止损,任一命中就停】
  1. 日志里出现 `nan` / `inf`;
  2. `avg50` 连续 **2 个打印行**(=100 步)> **90**(基线 ≈62【出处:HANDOFF.md §4】)。
- 止损后的动作(照抄):
  ```bat
  copy /Y D:\vscode_projects\ee_download\outputs\ckpt\last_clip1_pre_h1.pt D:\vscode_projects\ee_download\outputs\ckpt\last.pt
  python scripts/build_dataset.py --clip-norm 10
  ```
  然后照常按 §3 贴回(含触发止损那一段日志)。

## 3. 贴回清单(全部要,缺一项 = 实验未完成)

1. `续训:恢复 step=... epoch=...` 那一行(确认恢复点)。
2. 从恢复点到停止的【全部】`step NNNN ...` 日志行(每 50 步一行;500 步约 10 行)。
   新增的 `enc=`/`dec=` 列必须在(不在 = 没拉到新代码,先 git pull 重跑)。
3. step 8000 的 eval 输出块(parseable + 头两条样例预测)。
4. 若触发止损:触发点前后各 5 行 + 你执行的恢复命令原文。
5. 若发生任何报错/警告(体检、快照、"快路径不可用"),整块原文。

## 4. 判决(预登记判据,数据回来后按此执行,不许移动球门)

基线速率换算:-0.06/350 步 ≈ **-0.09 / 500 步**(A2S 200 窗均值)。

| 贴回的 ΔA2S(500 步) | 判决 | 后续 |
|---|---|---|
| ≤ **-0.20**(≥2× 基线) | **H1 成立** | 保持 `--clip-norm 25` 长跑 |
| ≥ **-0.12**(≤1.4× 基线,噪声内) | **H1 不成立** | 转 H2/H3(下行) |
| 介于两者 | 未决 | 同参数再跑 500 步补判 |

H1 不成立时的分流(用同一份贴回数据里的 enc=/dec= 列,不用再跑):
- `enc/dec` 比值长期 < **0.05** → encoder 几乎收不到梯度信号 → **H2a**(encoder 适应/lr)优先;
- `enc` 与 `dec` 同量级 → encoder 在正常学 → **H3**(耐心/规模:step 7600 仅占 100k 总日程的
  ~7.6%,cosine 调度此刻 lr 仍在 ~99% 峰值)优先,先继续长跑再观测。

判决人:规划端。判决材料:只用贴回原文,任何数字引用必须标出处(HANDOFF.md §0 铁律)。

---

## 5. 中期读数(2026-07-14,贴回 8000→8350,共 350/500 步)【出处:用户粘贴日志】

- 勾稽:7/7 行 gn ≈ √(enc²+dec²) 误差 ≤0.05 → enc/dec 分组正确。
- A2S 3.25→3.18,最小二乘斜率 **-0.104/500 步**(基线 -0.086/500)→ 落在"H1 不成立"带
  (≥-0.12),但窗口未满 500 步,**按预登记不提前判决**;8500 处正式判。
- enc/dec 比值 0.89~1.13(同量级)→ encoder 信号充足,预登记分流规则下 **H2a 被数据否定,
  H3(耐心/规模)升为首选**。
- 稳定性:avg50 59.3~67.0(基线 ≈62),无 nan、无连续 >90,止损未触发。
  首窗 gn avg=37.8 随后回落 ~23,与预告的 AdamW 二阶矩过渡期一致。
- ts=0.0000 的行属正常:日志的 sem/ts 是"该优化步最后一个 micro-batch"的瞬时值,
  纯 A2S/A2S_lite 桶没有时间戳 token → ts=0。趋势请读分方言列(200 窗),不读瞬时 ts。

### 补贴回(判决前必须补齐,缺口是规划端的:本卡忘了要求命令回显)
1. **启动命令原文**(确认 `--clip-norm 25` 确实在命令里)——或 git pull 后重启一次,
   新代码开头会打印`训练配置回显: clip_norm=...`一行,贴这行即可。
2. step 8400 / 8450 / 8500(及以后)的日志行,补满 ≥500 步窗口。

---

## 6. 判决:H1 不成立(2026-07-14 满窗)【出处:用户贴回 8050-8500 + 启动命令原文】

- 命令已确认:`--clip-norm 25` 在启动命令里(用户贴回 cmd 原文)。
- A2S 3.25(8050)→ 3.15(8500)。最小二乘斜率 **-0.113/500 步**,端点法 -0.111/500;
  基线 -0.086/500。按预登记判据(≥-0.12 → 不成立):**H1 不成立**。
  测得值离"不成立"线(-0.12)很近,但离"成立"线(-0.20)很远——放开裁剪没有加速训练,
  平台期与梯度裁剪无关。这与预登记先验一致(AdamW 对恒定梯度缩放近似不变)。
- enc/dec 比值全程 0.79~1.32,同量级 → encoder 信号充足,H2a(encoder 缺信号)一并否定。
- 全程稳定:无 nan,avg50 59~67,止损未触发。
- **处置:clip 保持 25 不改回**(我们这个架构的官方配方本来就不裁剪,留着省一次配置变更),
  转入 H3 观察路线(下节)。

## 7. H3 观察路线(判决后生效,执行端不改任何参数)

- 继续跑,什么都不用动。每次 eval(step 9000/10000/…)把 eval 输出块
  (parseable + 头两条样例预测)和前后各 3 行 step 日志贴回。
- **下次全面复盘:step 12000**,比较 8500→12000 的 A2S 斜率和样例预测质量。
- 升级到 H2 实验(lr 网格,第一格 dec→3e-4)的条件,现在提前声明:
  12000 处斜率比现在再衰减一半以上(慢于 -0.06/500)且 A2S 仍 >2.8;或连续两次复盘无任何进展。
- 硬止损规则不变(nan,或 avg50 连续 2 个打印行 >90)。
