# ROUND2_DATA —— 第二轮:数据先行(D53,用户战略拍板 2026-07-22)

## 决议

用户判断:数据量差距(≈1/8,TAST 1/25)使继续第一轮训练无意义 → **立即停训,
转数据建设期,建满后重训**。规划端原推荐"跑到双判决再定"被否决,如实记档;
第一轮权重(~68k)归档为基线,C2/C3 的独立判决随停训作废 —— 其数据直接并入二轮池
(二轮的对照 = 第一轮整体,不再逐杆归因)。

## 二轮池构成(目标 ≈ 论文的 1/4~1/5)

| 项 | 状态 | 量 |
|---|---|---|
| 主池 + C2 o10/o5 + C3 全池 + pdmxperf 二音色 | ✅ 装配自证(ROUND2_POOL_5_S2) | **614,710**(池 v2 终值;TAST 59,911 较一轮翻倍) |
| C1a 在线增广(增益/谱斜/噪声,标签安全) | ✅ 已接线(--augment-acoustic,默认关,开训单变量启用) | 乘数,零磁盘 |
| **去重回收波次(O8)**:上游 deduplicated 98.56% 假去重(PDMX_DEDUP_AUDIT) | ⛔ 三闸未过:磁盘 120+GB(现 91)/VN-unknown 冒烟/train-only 红线 | +142,346 曲 ≈ +183,773 段(估算) |
| 结构不匹配池映射修复(6,410 曲) | 💤 可选加长项(lenient 接线已为其铺路) | ≈+2 万行 |
| 16 音色商业 VST / DBD 方言 | ❓ 用户决策(O7,不阻塞) | — |

**校准闸(D54)已过**(均值 63.40,灰区人工复核放行)。**封池闸已过**(614,710 dry-run
自证,评测池冻结完好)。**当前唯一等待:O6 + O8 用户拍板 → 开训口令。**
若 O8 选"训中进池":restore 波次渲毕+QC 后随例行重启并入(C3 先例),**train-only 强制**
(preflight 显示其原生切分会扩 val/test —— 评测协议不动,禁止)。

## O6 已裁决(用户 2026-07-23):B-试验先行,硬顶 10k,不行转 A —— 预注册如下,不许挪

用户方案:"先看看 68k 继续的效果,最多再 10k,如果不好重头开始"。规划端评估采纳,理由:
①lr 非死尾:原 cosine(warmup 1500/max 100k/min_ratio 0.1,train.py)在 68k 处 0.315×峰、
78k 处 0.206×峰 —— 温模型的正常适应带;②判据族在同一 lr 带预注册过(C2,63k-71k),
检测灵敏度已知;③成本不对称:试验 ~1 天,失败照走 A,成功省 ~7-9 天。

**试验预注册(先于数据)**:
- 变量唯一 = 池 v1→**v3(753,304)**(D71 冻结;v2 版试验被 n=2 空读数错杀,VOID 不计,D72)。混比 D2 不变、**C1a 保持关**、lr 用原调度不改。评测已加 n<12 印 NA 守卫 + 生成长度/停因/快慢路径仪表。
- 起点 = outputs/ckpt/last.pt;起点步数以恢复横幅为准。**正典启动命令(D75 钉板):`python scripts/build_dataset.py --clip-norm 25 --lr-dec 3e-4`**(一轮工作点 = RESTART_C2.txt 存档回显;两次漏旗标启动的步数作废不计时)。
- 判决点 = 起点 + 10,000 步(用户硬顶)。到点【停进程】,判决归规划端。
- **成功**(→继续跑完二轮,C1a/restore 各随后续例行重启单变量进入):
  maestro Δpitch ≥ +0.03 连续 3 评(主判,C2 同族;一轮基线 = 25+ 评连零);
  或 AMT 真pitch 绝对 ≥ 0.25 且末 5 评单调不降(次判)。
- **失败**(→终止,转 A:canary 全新热启 + 按 v2 池重排 lr 计划):判决点两判均未命中。
- **灰 = 败**(预注册,不展期不加步 —— 硬顶是用户自己定的,防目标漂移)。

## O8 已裁决(用户 2026-07-23):A-训中进池

现池即刻开训不等 restore;restore 线前置三闸(磁盘 120+GB、VN-unknown 冒烟、train-only)
全过后渲染,渲毕 QC 过随例行重启并入(C3 先例)。

## 时间线

即刻(EXECUTOR 追加 8):停训归档 → 武装 s2 → 生成 o5 → C3 全池后台渲 → dry-run 对账。
规划端 24-48h:C1a 接线 + pdmxperf 二音色工具 + 二轮启动清单;然后 O6 拍板 → 开训。
C3 全池渲完(~4-6 天)若晚于开训:按既定实践,随例行重启中途进池。
