# EXPERIMENT_O4_MIX —— AMT 混比 0.30→0.22(50000 步复盘决议,用户已拍板)

## 背景与触发(判据是 D32 预登记的,没有移动过)

O4 预登记条件:step 50000 时 AMT Δsem 仍 ≈0 且 amt_f1 仍 0 → 提请用户拍板。
实测(autolog 45000-53000,九次 eval):maestro/AMT Δsem = +0.02/+0.00/+0.01/-0.01/
+0.01/+0.02/+0.00/+0.01/-0.00,**连续 25 次 eval 贴零**;amt_f1 全程 0.0。触发成立。

同期大盘健康(同源):A2S 滚动 2.72@38000 → 2.59@53050;nasap Δsem 升至 +0.23 新高
(真sem 0.76 新高);样本0 于 49000/50000 两次脱离兜底吐出真实乐谱(51000 起回落,
在校验边界震荡);parseable 0.04-0.10 平台。

**决议:用户批准 AMT 0.30→0.22**(2026-07-20,原话"我同意0.22")。
腾出的 0.08 按 35:15:20 等比还给三方言 → 生效混比
A2S 0.39 / A2S_lite 0.1671 / TAST 0.2229 / AMT 0.22(`mix_with_amt(0.22)`,tests_amt_mix.py 钉死)。

定性(诚实版):这不是治 AMT 的药,是止损——三成算力喂给一个 50k 步不读音频、
F1=0 的分支,机会成本太高;TAST 已证明音高能力在。AMT 可能因此更晚醒,接受。

## 执行(一次重启,三件事同车:新混比 + 召回 +7,501 段进池 + 例行续训)

```bat
:: 停当前训练进程(按命令行精确匹配,别 pkill)
git pull --rebase --autostash
cmd /c "D:\ProgramData\envs\nemo_test\python.exe -u D:\vscode_projects\ee_download\Rubato\scripts\build_dataset.py --clip-norm 25 --lr-dec 3e-4 --amt-mix 0.22 >> D:\vscode_projects\ee_download\reports\train_full.log 2>&1"
```

checkpoint 自动从 last.pt 续(~53050),不从头训。

## 贴回清单(缺一不可)

1. 启动**装配统计整块**(`=== 装配统计 ===` 到 `train=...` 行)——这同时是
   RECALL 终验收:pdmx kept 应比上次开训涨 ≈+7,501,no_audio 相应大降。
2. **训练配置回显行**(第一行,应含 `mix=A2S:0.390,A2S_lite:0.167,AMT:0.220,TAST:0.223`;
   若显示 `mix=D2纸面` = 旗子没带上,停下来贴回)。
3. **epoch0 混比报告行**(`epoch0 混比报告: ...quota...`)——配额落地的自证。
4. 恢复行(`续训:恢复 step=...`)。
5. 此后照旧:逢 eval push autolog。

## 预登记判决(写在数据到来之前;基线钉死于 53050,不许事后挪)

- 基线:A2S 滚动 2.59@53050(STEP_53000.txt);近 15.1k 步斜率 ≈-0.0086/1000
  (2.72@38000→2.59@53050);无干预外推 → 重启后 +8000 步(≈61000)时 A2S ≈2.52。
- **成功**:61000 时 A2S 滚动 ≤ 2.49(明确好于外推);
- **失败**:> 2.52(没跑赢无干预外推)→ 回滚:去掉 --amt-mix 恢复 0.30,从当时 ckpt 续训;
- 中间(2.49-2.52)= 灰区,延长观察到 69000 再判,判据不变。
- 护栏(任一破线单独上报,不等复盘):nasap Δsem 连续 5 eval 均值 < +0.10;
  pdmx 强样本 < +0.15;parseable 8-eval 均值 < 0.06;eotP0 > 0.5。
- AMT 侧只记录不判决:若 AMT Δsem 连续 3 eval ≥ +0.05(减了算力反而醒)→ 单独报告。
- **混杂声明**:同车进池的 +7,501 pdmx 段无法与混比效应分离(D37 预定,不为此推迟)。
  本判决口径 = "混比+新数据"打包 vs 纯外推基线;若成功,归因不拆分。

## 执行端主张核查(2026-07-20,commit 3ce1c2f)

commit 标题 "recall data active / 2x improvement / A2S=2.56" 三项均不采纳:
- 召回数据**尚未进池**——50000-53000 eval 时间戳连续无重启断点,无装配统计推送;
  新段只能在重启装配时被发现,而重启是本卡这次才做;
- "+0.22 = 2x" 不成立——强样本 Δsem 自 36000 起就在 +0.14~0.27 带内震荡,+0.22 在带内;
- A2S=2.56 无出处——贴回文件 STEP_53000.txt 里是 2.59@53050。
