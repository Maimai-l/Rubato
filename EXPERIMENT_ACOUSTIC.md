# EXPERIMENT_ACOUSTIC —— 声学多样性补救计划(阶段 C 总卡,D47)

背景:REF_DATA_SCALE 对账 —— 与论文的差距主要是"每份内容的声学/时间视角数"
(论文每曲 16 音色变体 + 真实音频密集重叠切窗;我们每曲 1 个固定渲染 + 单遍切窗)。
症状对得上:模型读合成音频强、读真实录音弱(maestro Δpitch≈0)。用户已拍板做补救。

## 总原则(O4 的教训直接搬来)

- **一次重启只进一个变量**,每步预登记判据,失败可回退(数据杆的回退 = 下次重启去掉新行);
- 方言混比 D2(0.35/0.15/0.20/0.30)全程不动 —— 变的是池子内容,不是配比;
- 新数据全部沿用既有防线:泄漏黑名单、时长守卫、内容审计。

## 分级计划

### C0 渲染质检 + 库存审计【已关案,2026-07-21 @3b102d1】

全量判定:38,252 曲全查,疑似截断 **3 曲(0.008%,噪声级,不重渲)**,曲号存档:
QmSYDrDRgzTQpgwdGkGHvU8Fe8CykKToz8Yp7e2uzTqdXo(-4.1s)/ QmUpns6nFmxZPifMYujCnHqjZerZRfCzAoiAY6moV1ByHR(-3.1s)
/ QmQMS66EnbPCt9mt9XJELBT1kfVGLhfXGhyF55sSGLggAH(-1.9s,327s 长曲,疑为尾音收尾约定差)。
音色分布:每曲恰 1 组合坐实,96 种组合,Salamander 头部;**1,298 曲 bone_dry 干声
= C1b 的天然零成本试点池**。maestro 整曲 199h 全在(C2 原料)。duration_check 质检门
自 D45 揪出后首次真正行使职能,门后干净 —— 本节结案。

### 【存档】C0 原始交付说明

`python scripts/audit_render_qc.py` → reports/render_qc.md,三件事:
① 时长对账(补上从未接线的 duration_check 门,D45):疑似截断渲染计数,>0 则入重渲队列;
② 音色分布:确定性哈希重算每曲的源/预设组合(量化"每曲 1 音色"现状与池间均衡);
③ maestro_audio 整曲库存(小时数)= C2 原料盘点。
验收 = 报告三节齐全;截断计数与最重案例点名。

### C2 真实音频密集重叠切窗【已交付 + 冒烟通过;R1(71000)进池】

冒烟验收(@e9afdbc):5 场限额 → 4 train 场产 501 窗/501 标签/0 失败,skip_nontrain=1
(冻结守卫实锤),临时名输出未武装。外推全量 ≈12 万行,与预期账吻合。只待 71000 口令。

实现(tests_c2_offset 四项钉死):`segment.shift_events`(事件前移,踏板保持状态合成初始
事件带入,跨界音按既有口径丢)+ `s6_amt_windows.py --offset 10`(同一台切窗机、同一 token
实测把关;win 坐标回写 +offset,utt_id 带 `_o10`,输出 maestro_amt_windows_o10.jsonl)+
build_dataset 条件挂载(文件存在即进池)。

**评测冻结保证**:偏移模式强制只产 train 切分(skip_nontrain 计数),val/test 窗系一字不动,
eval 的 48 样本与全部历史严格可比。

**进池时机(D49 修订,用户质询后改)**:~~等 71000 复盘~~ → **立即进池**。
原"留干净空窗"的顾虑不成立:主判据 Δpitch 的基线是连续 25 次 eval 的持续零,且 0.30
配比在 38k-53k 从未使其脱零,不构成混淆源;而 cosine lr 衰减使晚进 7k 步 = C2 学习窗
缩水 24%,旧池已过 ~13 epoch,再嚼无益。判决窗改为 **进池步 +8000**,判据本身不变。

预期规模:train 演奏 ~962 场 → 偏移窗 ≈ +11 万行,AMT train 池 ~11.6 万 → ~23 万
(混比 0.30 不变 → 配额不变,过采样 0.43→~0.21,每 epoch 独特 AMT 内容近翻倍)。
判据(R1+8000,预登记不变):主看 maestro/AMT Δpitch 连续 3 eval ≥+0.03(首次脱零)
与 amt_f1;护栏 = A2S 斜率不劣于外推、nasap/pdmx Δsem 不降带。失败回退 = 删 o10 文件重启。

**R1 落位与基线钉死(2026-07-22,@76d6429/84a3642)**:恢复步 **63600** → 判决点 **71600**。
进池验收全对:生成 114,588 窗(skip_nontrain=314 = 官方非 train 场次,分毫不差),
maestro rows 144,087→258,658(差=labels 数精确),TOTAL 384,862。
**判决用基线带(55k-63k autolog,防归因错误)**:maestro Δsem −0.01~+0.02 / Δpitch
−0.04~+0.00 / 静sem 0.71-0.73 / 真pitch 0.15-0.19;nasap Δpitch +0.44~+0.60(TAST 本就
读音高,仪表新≠现象新 —— 执行端 C2-eval1 标题把带内值 +0.57 归因 C2,不采纳);
pdmx 弱样本 +0.28~+0.49、强样本 +0.24~+0.39。
**C2 后首 eval(~64600)唯一出带值:maestro 静sem 0.65(带外)、Δsem +0.07(26 评首次)**
—— 与"新内容抽走背文本拐杖"机制一致,单点不判,连续性看后续 eval。

机制与论文同源(30h nASAP → 214k、159h MAESTRO → 804k 的那个乘数):整曲 flac 已全在盘
(work/maestro_audio),装配器本就支持 win= 窗读 —— **只加标签行,不加音频字节**。
v1 保守起步:现有窗系 + 一组 +10s 偏移窗 ≈ 真实音频视角 ×2(论文 ~5-30×,先证方向再加码)。
实现 = 驱动既有 maestro AMT 窗切器与 s7 nasap 标签器以偏移参数重跑,新行 utt_id 带 `_o10` 后缀,
黑名单/守卫复用。**预登记判据(R1+8000 步)**:主看 maestro/AMT Δpitch 是否首次脱零
(连续 3 eval ≥+0.03)与 amt_f1;护栏 = A2S 滚动斜率不劣于外推、nasap/pdmx Δsem 不降带。
失败回退 = 下次重启去掉偏移行。

### C3 武装判据(预登记于武装之前,2026-07-22;D52)

- **武装时机**:71600 判决后的同一重启(与 C2 处置/泄漏生效同车)。归因分工:AMT 侧仪表
  (Δpitch/amt_f1/maestro 探针)归 C2 处置解读;**A2S/pdmx 侧仪表归 C3**;泄漏隔离不动评测池。
- **基线(武装时以实值定锚)**:A2S 滚动值@71600 与 53k→71.6k 实测斜率(≈−0.007/千步);
  pdmx 探针带(近十评):弱样本 Δsem +0.04~0.19 / Δpitch +0.28~0.49,强样本 Δsem +0.14~0.38
  / Δpitch +0.24~0.54;parseable 带 0.04~0.15。
- **判决 @ 武装+8000**:
  - 成功:A2S 滚动 ≤ 外推值 − 0.02,或 parseable 八评均值 ≥ 0.15;
  - 失败(=损伤):A2S 劣于外推 > 0.02,或 pdmx 探针带跌破(弱样本 Δsem 连续 3 评 < 0);
    处置 = staging 改回,卸载 s2,不动权重;
  - 其余 = 中性:s2 保留(纯数据多样性,保留零成本),记"未证增益",C1a 顺延开庭。
- 验收:武装重启的装配统计 pdmx kept 119,106 → ≈153,600(+34,556 段)。

### C1a 湿声安全在线增广【C2 判决后,R2 重启启用】

增益/EQ 倾斜/轻噪声(不动音高/时间戳标签;**不含混响** —— 湿声上叠混响 = 双重房间,
此维度被 C1b 前提卡死)。确定性 seed(utt,epoch),旗子门控默认关,R2 单独启用单独判。

### C3 免费音源离线副本【待 C0 数字回来请用户拍板规模】

现有 5 免费音源对一个子集渲 ×2-4 副本(例:30k 段 ×2 源 ≈ +24GB 磁盘 + 数天 CPU,
与训练并行,召回战役已验证此作业模式)。拍板项:子集大小 × 副本数(= 磁盘/CPU 预算)。

### C1b 干声重渲(房间维度真解)/ C4 合成 AMT 池【挂账】

C1b 需把渲染改为干声 + 在线上房间(前任半成品的完整版)= 全量重渲级成本;
C4 给 AMT 加合成池(论文 436k 那块)= 渲染 + 装配改造。均待 C2/C1a/C3 数据后再议。

## 时间线

C0 今天;C2 生成器数日内交付(附测试),进池搭 71000 复盘后的例行重启(R1);
R1+8000 判 C2 → 决定 C1a(R2)与 C3 规模请示。
