# EXPERIMENT_LISTEN —— "不听音频"作战梯子(D79,项目中心问题的标准应对)

## 病理(全部结论有案可查,不是猜测)

模型 = 音频编码器 + 文本解码器(交叉注意力连接)。病灶不是"注意力坏了"——
**合成侧探针证明听觉通路机械上是好的**(nasap/pdmx Δpitch +0.3~+0.65,D28/D29 起持续)。
病灶是**抄近路竞争**:乐谱文本高度可预测(拍号/调号/音型/重复),纯语言先验就能把
loss 压下去一大截;外加一轮 13 epoch 的高重复池允许**背题**(记住训练窗的音高序列)。
两条近路都比"从音频里读音高"便宜 → 梯度走近路,maestro 真pitch 冻在 0.15-0.20
(≈瞎猜水平)。复读循环是同一病的生成端症状:没在听的时候,只剩语言先验在写。

## 梯子(D81 改配方制:温和剂量打包,重项单独把关;打包后以合成侧基线为护栏)

| 级 | 手段 | 机理(为什么打这里) | 状态 | 触发条件 |
|---|---|---|---|---|
| 0 | **大池 + 从零起跑 + C1a 增广**(二轮基线,D80 用户令合并) | 过采 2.4×→0.6× 饿死背题;增广饿死背音频;数据在习惯成形前在场 | **跑着(增广自第 0 步开)** | — |
| 1b | **音高 token 加权 loss**(×2.5) | "音高只占 loss 小头"是近路的算术基础;CE 按音高 piece 掩码加权(均值归一) | **已建成,二轮自第 0 步生效**(pv= 列直读;61k 处 2.81→2.54 在降) | — |
| 1c | **遮上文 = 教师强制输入 dropout**(低比率+爬坡) | 随机把解码器文本上文遮成 unk → 语言先验断供,被迫查编码器;直打 D85 判定的曝光偏差 | **首门未过(63.8k,p=0.10):loss +20.8% 超 +10% 限,未晋级** —— 但通胀是机械的(目标按设计变难),100 步内快速适应非失稳;ts 通道贡献 2.6× 于 sem(而 TS_MISSING 恰是拒因第二名) | **半剂量 p=0.05 重门**(追加 30,门限自实测重标 ≤×1.12) |
| 2a | **编码器 AMT 辅助头**(帧级 onset/occupancy/offset) | 绕开文本 loss,直接逼编码器携带音高信息 | **已建成并已门测:头学得动(af1 +45%,aux BCE −33%)但不迁移到 decoder** → 判决:别加大头,打 decoder(转 2c) | 已触发过;除非 2c 也败,不再加码 |
| 2b | **AMT 课程表**(开局若干步临时提高 AMT 配比) | 让听觉梯度先占场,文本任务后进 | 动 D2 混比,**需用户批** | 2c 门测失败再议 |
| 2c | **交叉注意力音频依赖损失**(批内错配 encoder 状态,margin 罚) | 2a 判决的直接后继:同批 roll 音频做第二次 decoder forward,错音频算内容 CE 必须比对音频差出 margin | **首门实测改判(63.8k):gap=+0.83~0.86 ≫ margin 0.1 ⇒ 罚项空转,+77% 步时只买了仪表 —— decoder 在合成域并不聋,聋是真钢琴域特异**。训练版搁置;降级为纯仪表 --audio-dep-monitor-every(no_grad 每 N 步出 ad=,≈+1.5%@N=50) | 定向真音频域版(按域掩码)另立项,需批内域标签 |
| R3 | **decoder 形式语言预训练**(round-3 设计要素,D90/D91/D92) | 论文只借 Canary 架构、权重从零(§3 原文);表示节把音高状态钉为 k-Shuffle-Dyck 并引 pre-pretraining 论文(用户拍出的线索)—— 头号拒因 DYCK/MEASURE 恰是这两个形式性质 | **已备货(D92)**:20k 步跑完,自由续写 0/4→3/4、拒因收缩到 MEASURE×1 = decoder 纯语法域学会收口;decoder_init.pt 合格入库(SHA 6DFA62AF…);n=48 复评挂暂停窗 | 100k 终点判读(D88⑦)触发 round-3 时 --decoder-init 入场,对照 = round-2 同步数历史曲线 |
| 3 | 架构级(交叉注意力门控等) | 最后手段 | 不建 | 前面全败再议 |

## 二轮中期更新(step≈61k,证据 reports/DECODE_AB_STEP61200.md + eval_autolog)

- **部分见效**:maestro 真pitch 0.28-0.34(一轮冻结区 0.15-0.20 已脱冻);amt_f1 首次非零
  (2.6-5.5%);pv 2.81→2.54 在降 —— 教师强制侧在学。
- **剩余病灶改判(D85)**:parseable=0.00(真值,n=48)不是超参问题而是**曝光偏差**:
  自由生成收不了口(循环/缺时间戳/声部不闭合)。beam/greedy、域 prompt、900 上限、
  验证器过严已全部排除(4 条金标同验证器在预算内通过)。
- **对策已备**:解码侧 = 扫参工具(--decode-abtest-rep/-eot,同 ckpt 网格比较重复惩罚
  ×终止符加成);训练侧 = 1c 遮上文 + 2c 音频依赖损失,各经 100 步 A/B 门进场。
- **63.8k 暂停窗判定(D87,证据 reports/D86_PAUSE_WINDOW_EXPERIMENT_2026-08-04.md)**:
  扫参 16 臂 13 臂 0/24、DYCK 不动 → 解码侧关案(eot=4 的"救回"经 JSON 取证为
  49-token 截肢式技术通过,NED 0.83,不作配方推荐);
  双杆门未过(tc +76.6%/loss +20.8%),主线已从未动的 63800 快照素跑恢复;
  **首个 decoder 音频依赖直读数 ad=+0.83~0.86**(合成域不聋,聋在真钢琴域);
  1c 半剂量重门 + ad 纯仪表进场提案见 EXECUTOR 追加 30。
- **70k 训练集自由解码(D88,TRAIN_FREE_DECODE_STEP70000.json)——病理定稿**:
  训练样本(已见 ~5 epoch)parseable 同样 ≈0(real 1/48,synth 2/48)⇒
  **数据量/泛化解释出局,parseable≈0 = 序列生成能力问题**。拒因谱分裂给出两层:
  〔层1〕合成域括号/终止/时间戳大多正常,死在 MEASURE 自洽(计数类能力,误差复利:
  逐 token 99% 纪律 → 400-token 窗只有 ~2% 全过;parseable 是悬崖指标,与 sem 2.38
  仍高于 2.0 里程碑互证"还没到")—— 治法 = 步数 + 1c;〔层2〕真音频上纪律全面崩
  (音频条件化崩溃,与 maestro 聋区同源)—— 治法 = 真实域数据/音色(O7 类资源)。
  预注册预测:素跑 100k 终点 parseable 大概率仍 <10%,届时按 D88⑦ 处置,不判死。

## 仪表(现成,每 1000 步自动出数)

- **主表**:maestro 探针 Δpitch / 真pitch(冻结区 = 0.15-0.20;脱冻 = 连续 3 评 ≥+0.03);
- **晴雨表**:解码撞 900 上限的比例(gen-stats stop=cap 占比)——学会听 → 该降;
- **优雅针 raw_ned=(D89,LEGATO 测量制度移植)**:自由生成逐窗原文(验证拒绝也保留)
  剥戳拼接对参照的 NED,全样本进分母 —— parseable 悬崖之前唯一能看到自由生成内容
  进步的连续读数;应随训练下降,与 parseable 跳变互补;
- **新列**:pv=(音高 CE,1b)/ id=(遮上文实际命中率,1c)/ ad=(错配−匹配 CE gap,
  decoder 听没听的直读数;63.8k 基准 +0.83~0.86;仪表模式 --audio-dep-monitor-every
  下每 N 步出数 —— 若随训练下滑 = decoder 在丢音频通道,红灯);
- 佐证:maestro Δsem(试验里已见 +0.01→+0.08 微弱正效应,数据方向不是零)。

## 节奏

**round-2 已由用户令于 ~73k 终止(D93,2026-08-05)**:ckpt 归档 ckpt_r2_73k,其全程
评测曲线转为 round-3 的历史对照组;D88⑦ 的 100k 预测按"弃考"存档不判对错。
**round-3 = R3 行激活**:round-2 配方 + 唯一变量 --decoder-init(强化预训练 v2 工件,
门不过则用已合格 v1);判决不等终点 —— 20k 预登记判决点(DYCK 拒因数 ≤ round-2@20k
一半 / raw_ned ≤0.85 / 探针 sem 劣化 ≤5%,两胜续跑全败停)。1c 保持关,留作后续
独立门。**每一级仍是实验,不是信仰。**
