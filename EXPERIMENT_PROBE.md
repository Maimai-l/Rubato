# EXPERIMENT_PROBE —— 教师强制探针(查"生成为什么停在开头")

> 给执行端:这次**不改任何训练参数**,只拉新代码重启。新代码会在每次 eval 里
> 多打几行"探针"诊断,把它们贴回来就行。

## 背景

优化器侧三个嫌疑已全部用实验排除(裁剪 H1、encoder 信号 H2a、decoder lr H2),
loss 还在慢慢降,但 eval 的自由生成始终只吐 `'|4/4k0'` 就停。这有两种可能的病,
损失曲线上看不出区别:

- **病 A(模型没学会)**:就算把正确答案的前文全喂给它,它也猜不中下一个 token。
  → 无药,继续训,但从此有了比 parseable 灵敏得多的进度表(命中率会先于 parseable 动)。
- **病 B(暴露偏置/解码问题)**:喂正确前文时它预测得不错,只是自己生成时一步错步步错,
  或者一开口就想输出结束符。→ 有药可修(解码约束、scheduled sampling 等),不用干等。

探针做的事:每次 eval 拿一个验证样本,把参照答案按训练时的格式喂给模型,
逐位统计"下一个 token"的命中率,并量出第一个内容位上结束符的概率。

## 操作

```bat
git pull --rebase --autostash
(停掉当前训练,Ctrl+C 即可 —— 参数不变,不用备份)
python scripts/build_dataset.py --clip-norm 25 --lr-dec 3e-4
```

跑过 **两次 eval**(每 1000 步一次)即可贴回,继续挂着跑不用停。

## 贴回(缺一不算)

每次 eval 的这些行,**原样整段**:
1. `eval 探针[...]: acc=... 前缀acc=... eotP@首位=... n=...`
2. `eval 探针argmax: '...'` 和 `eval 探针参照: '...'`(两行对照,看它想说什么 vs 该说什么)
3. `eval 样本预测[0/1]` 两行
4. `eval 汇总: ...` 一行(新增的,单行带全所有关键数)
5. 配置回显行 + 续训行

## 判读标准(提前声明)

看**前缀acc**(前 32 个内容位的命中率),连续两次 eval 取均值:
- ≥ 0.60 → 病 B(暴露偏置/解码),转解码侧修复,规划端出方案。
- ≤ 0.30 → 病 A(模型未熟),继续长跑,探针命中率成为常设进度指标。
- 之间 → 再看两次 eval 的趋势;eotP@首位 > 0.1 时病 B 加权。

## 报告规矩(新增,对应本次事故)

- **报告文件只增不删**。cd996eb 把 H2_steps.txt 的 eval 段整段删掉了(已由规划端从
  git 历史恢复)。日志是证据,"final"也不许覆盖或删除之前的内容,追加即可。
- 摘录日志时,所有以 `  eval` 开头的行都要保留(包括 探针/样本预测/汇总)。

---

## 第二版(2026-07-15 深夜):探针 device bug 已修 + 真相修正 + 吞错现场

**重要修正**:`样本0='|4/4k0'` 不是模型输出 —— 它是 `_EMPTY_A2S` 兜底常量(infer.py:24)。
本轮 eval `empty=1.0` 说明 48 个样本全部走了兜底:推理管线在(a)解码异常、(b)输出过不了
validate、(c)顶层异常三处**静默吞错**,我们之前把兜底常量误读成"模型只会输出开头"。
"model generates EOS immediately"(ff7b331 提交信息)目前没有任何数据支撑,不作数。

已修/已加(git pull 后生效):
1. 探针 device bug(GPU/CPU 混比较)已修 —— 上轮探针没跑成,这轮会出数。
2. 三层吞错全部装了现场记录,eval 出现兜底时自动打印:
   - `eval 解码现场: {stage: validate_reject, viol: [...], raw: '模型真实输出前160字'}`
     ——**模型到底吐了什么、为什么被拒**,第一次可见;
   - `eval 兜底异常: ...traceback...`(顶层异常时)。

### 操作(同前,重跑一次)

```bat
git pull --rebase --autostash
(停训练,Ctrl+C)
python scripts/build_dataset.py --clip-norm 25 --lr-dec 3e-4
```

### 贴回(跑过 1-2 次 eval 即可)

配置回显行、续训行,以及 eval 的全部 `  eval` 开头行 —— 这次重点是三样:
`探针acc/前缀acc/eotP`、`解码现场`(模型真实输出)、`兜底异常`(若有)。

### 报告文件规矩升级(第二次违反后,改成更简单的规则)

**不要再编辑任何已存在的报告文件。每次新报告写新文件**,编号递增:
`reports/PROBE_RESULT_2.txt`、`reports/PROBE_RESULT_3.txt`……旧文件一个字都不动。
