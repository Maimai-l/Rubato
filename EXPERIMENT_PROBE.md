# EXPERIMENT_PROBE —— 教师强制探针(查"生成为什么停在开头")

> 给执行端:这次**不改任何训练参数**,只拉新代码重启。新代码会在每次 eval 里
> 多打几行"探针"诊断,把它们贴回来就行。

## 背景

优化器侧三个嫌疑已全部用实验排除(裁剪 H1、encoder 信号 H2a、decoder lr H2),
loss 还在慢慢降,但 eval 的自由生成始终只吐 `'|4/4k0'` 就停。这有两种可能的病,
损失曲线上看不出区别:

- **病 A(模型没学会)**:就算把正确答案的前文全喂给它,它也猜不中下一个 token。
  → 无药,继续训,但从此有了比 parseable 灵敏得多的进度表(命中率会先于 parseable 动)。
- **病 B(暴露偏置/解码问题)**:喂正确前文时它预测得不错,只是自己生成时一步错步步错,
  或者一开口就想输出结束符。→ 有药可修(解码约束、scheduled sampling 等),不用干等。

探针做的事:每次 eval 拿一个验证样本,把参照答案按训练时的格式喂给模型,
逐位统计"下一个 token"的命中率,并量出第一个内容位上结束符的概率。

## 操作

```bat
git pull --rebase --autostash
(停掉当前训练,Ctrl+C 即可 —— 参数不变,不用备份)
python scripts/build_dataset.py --clip-norm 25 --lr-dec 3e-4
```

跑过 **两次 eval**(每 1000 步一次)即可贴回,继续挂着跑不用停。

## 贴回(缺一不算)

每次 eval 的这些行,**原样整段**:
1. `eval 探针[...]: acc=... 前缀acc=... eotP@首位=... n=...`
2. `eval 探针argmax: '...'` 和 `eval 探针参照: '...'`(两行对照,看它想说什么 vs 该说什么)
3. `eval 样本预测[0/1]` 两行
4. `eval 汇总: ...` 一行(新增的,单行带全所有关键数)
5. 配置回显行 + 续训行

## 判读标准(提前声明)

看**前缀acc**(前 32 个内容位的命中率),连续两次 eval 取均值:
- ≥ 0.60 → 病 B(暴露偏置/解码),转解码侧修复,规划端出方案。
- ≤ 0.30 → 病 A(模型未熟),继续长跑,探针命中率成为常设进度指标。
- 之间 → 再看两次 eval 的趋势;eotP@首位 > 0.1 时病 B 加权。

## 报告规矩(新增,对应本次事故)

- **报告文件只增不删**。cd996eb 把 H2_steps.txt 的 eval 段整段删掉了(已由规划端从
  git 历史恢复)。日志是证据,"final"也不许覆盖或删除之前的内容,追加即可。
- 摘录日志时,所有以 `  eval` 开头的行都要保留(包括 探针/样本预测/汇总)。

---

## 第二版(2026-07-15 深夜):探针 device bug 已修 + 真相修正 + 吞错现场

**重要修正**:`样本0='|4/4k0'` 不是模型输出 —— 它是 `_EMPTY_A2S` 兜底常量(infer.py:24)。
本轮 eval `empty=1.0` 说明 48 个样本全部走了兜底:推理管线在(a)解码异常、(b)输出过不了
validate、(c)顶层异常三处**静默吞错**,我们之前把兜底常量误读成"模型只会输出开头"。
"model generates EOS immediately"(ff7b331 提交信息)目前没有任何数据支撑,不作数。

已修/已加(git pull 后生效):
1. 探针 device bug(GPU/CPU 混比较)已修 —— 上轮探针没跑成,这轮会出数。
2. 三层吞错全部装了现场记录,eval 出现兜底时自动打印:
   - `eval 解码现场: {stage: validate_reject, viol: [...], raw: '模型真实输出前160字'}`
     ——**模型到底吐了什么、为什么被拒**,第一次可见;
   - `eval 兜底异常: ...traceback...`(顶层异常时)。

### 操作(同前,重跑一次)

```bat
git pull --rebase --autostash
(停训练,Ctrl+C)
python scripts/build_dataset.py --clip-norm 25 --lr-dec 3e-4
```

### 贴回(跑过 1-2 次 eval 即可)

配置回显行、续训行,以及 eval 的全部 `  eval` 开头行 —— 这次重点是三样:
`探针acc/前缀acc/eotP`、`解码现场`(模型真实输出)、`兜底异常`(若有)。

### 报告文件规矩升级(第二次违反后,改成更简单的规则)

**不要再编辑任何已存在的报告文件。每次新报告写新文件**,编号递增:
`reports/PROBE_RESULT_2.txt`、`reports/PROBE_RESULT_3.txt`……旧文件一个字都不动。

---

## 探针第一批数据的判读(2026-07-15 深夜,ccbcc72)【出处:reports/PROBE_RESULT.txt】

数据:`acc=0.40 前缀acc=0.38 eotP@首位=0.0001 n=389`,argmax/参照对照各一行。

1. **"模型开口就想输出结束符"的说法正式死亡**:eotP=0.0001,首个内容位上 EOS 概率万分之一。
2. **前缀acc=0.38 落在预登记的中间带**(≥0.60 病 B / ≤0.30 病 A)——执行端提交信息里
   "decoding bias confirmed" **不获采纳**,判据没到,按卡上规则要再看一次 eval 的趋势。
3. 定性读 argmax/参照对照(第一次看到模型真实想说什么):**格式骨架已学会**
   (小节线+拍号、双手谱表标记、时间戳穿插位置、时值分数全对路),**时间戳跟着音频走**
   (0.38/0.48/0.68 vs 参照 0.28/0.56/0.83,量级对、单调),**音高与调号还不准**
   (k0 vs k-4,音名多数错)。这是"学到一半"的典型面貌 —— 自由生成过不了严格校验并不奇怪。
4. 这份报告是 eval 跑到 24/48 时中途推的,**缺 `解码现场` 行和 `汇总` 行** ——
   自由生成的真实输出与被拒原因仍未见到,这是下一份必须有的。

## 上报方式改革(生效于 git pull 后,第三次改写旧文件之后收权)

eval 的全部证据行现在**由代码自动追加写入 `reports/eval_autolog.md`**(带 step 与时间头)。
执行端上报训练情况 = 三条命令,不再打开任何文件:
```bat
git add reports/eval_autolog.md
git commit -m "eval autolog"
git push
```
**不要编辑这个文件**,也不用再手写 PROBE_RESULT 之类 —— 代码写的才算证据。

### 下一份要的东西(跑过下一次 eval 后)
push 一次 eval_autolog.md 即可,里面自动含:探针三行(第二个数据点,判中间带趋势)、
解码现场(模型自由生成的原始输出+违规项)、样本预测、汇总、指标行。

---

## 20k 复盘(2026-07-16,探针 4 个数据点 + 解码现场 3 份)【出处:eval_autolog.md + PROBE_RESULT_2-5】

1. **探针持平**:前缀acc 0.38/0.31/0.34/0.34,总 acc 钉在 0.40-0.41 —— 17000→20000
   三千步教师强制命中率零进步。中间带持平,病 A/病 B 判据都没命中。
2. **解码现场(自由生成的真面目)**:不是空、不是乱码,是**复读机** ——
   两个和弦无限交替(a2F3/f3A3、d-3G-3/g-3D-3、c4C4 循环),时间戳单调走,最终
   Dyck 配对断裂/缺终止小节线被校验拒。解码胶水没病:模型内容分布未熟,greedy 落进循环。
3. **amt_f1=0.0 首次可见**:AMT 方言自由生成同样不成谱 —— 生成侧未熟是全方言的,非 A2S 特有。
4. **A2S 训练损失 16600→20850 净零**(2.93→2.92,中途 2.87 再弹回);TAST/A2S_lite/AMT 微降。
   执行端"反弹因 lrD 衰减"不采纳:cosine 到此仅衰 8%,反弹在 200 窗噪声内。
5. 表扬:autolog 与编号文件这次都执行到位。

**焦点问题升级**:时间戳一直跟得上音频,音高/调号 20k 步了还是全错 ——
**decoder 到底有没有从音频里读"内容"?** 新版探针(git pull 生效)每次 eval 自动做
【静音对照】:同一参照序列,真音频测一遍、全零音频测一遍,报 `Δsem`(语义 token 命中率差),
并把命中率拆成 sem=(音高/时值/结构)与 ts=(时间戳)两列。

### 判读(预登记,数据回来前写死)
连续两次 eval 的 Δsem 均值:
- **≤ 0.02** → 病 C:decoder 根本没在用音频预测内容,时间戳的 acc 是靠"单调递增先验"撑的
  → 转查 encoder 特征/cross-attention(规划端出下一张卡,候选:SpecAugment 配置、
  encoder 冻结对照、cross-attention 权重可视化)。
- **≥ 0.06** → 在读音频,只是内容未熟 → 病 A 成立:继续长跑,探针 sem 列为主进度表;
  可另加"仅监控用"的重复惩罚解码让 parseable 提前脱 0(不影响训练)。
- 之间 → 再取两次 eval 平均。

### 操作
```bat
git pull --rebase --autostash
(停训练,Ctrl+C,参数不变)
python scripts/build_dataset.py --clip-norm 25 --lr-dec 3e-4
```
跑过 1-2 次 eval,push 一次 `reports/eval_autolog.md` 即可。

---

## 22000 读数与升级(2026-07-16)【出处:eval_autolog.md @ddf0f20】

**Δsem = 0.00,静音前后 acc/sem/ts 三指标逐位相同 —— 病 C(decoder 没在读音频)升为头号假设。**
注意连 ts 命中率(0.08)都不随音频变:之前"时间戳跟着音频走"的印象很可能是"单调递增先验"
的错觉。若坐实,连冒烟测试都要重新解读:32 样本过拟合纯靠文本记忆也能达标,它从未证明过音频通路。

**但本轮不判**,两个原因:
1. 三指标精确相同也可能意味着对照实验本身没生效 —— 证据须自证输入不同(本次升级已加);
2. 探针没有长度护栏(规划端的 bug):第二个样本参照超 1024 位置上限 → CUDA device-side
   assert → **上下文中毒,本轮 eval 之后的全部解码报废**(empty=1.0/amt_f1=0.0 是崩溃产物,
   不是读数)。执行端"病C confirmed"暂不采纳 —— 按预登记要两次 eval,且要下述自证。

已修/已加(git pull 生效):
- 长度护栏:参照超 1000 tok 自动截断(根治 CUDA assert);
- 探针行带 `rms=`(波形能量,证明静音/真音频输入确实不同);
- 探针行带 `enc帧=`/`enc_std=`(encoder 侧体征);
- 新增第三对照【错配音频】:样本0 的谱 × 样本1 的音频(分布内真钢琴声,比静音更严)。

### 判定矩阵(预登记,下一次干净的 eval 出来按此定案)

| 观测 | 判决 |
|---|---|
| `enc帧=0` 或 `enc_std≈0` | **病 C-结构**:encoder mask/特征管线坏,decoder 从结构上看不到音频。当场定案 |
| enc_std(真)≠enc_std(静) 且 Δsem≈0 且 错配 acc ≈ 原 acc | **病 C-忽略**:声学前端活着,decoder 学会了无视它(纯文本 LM)。定案 |
| Δsem ≥0.06 或 错配 acc 明显低于原 acc | 22000 的 0.00 是假象 → 回病 A(在读音频),继续长跑 |
| rms 或 enc_std 静音/真音频相同 | 对照实验实现仍有诈,停判,贴回全部行等规划端 |

### 操作(再来一次)
```bat
git pull --rebase --autostash
(停训练重启,参数不变)
python scripts/build_dataset.py --clip-norm 25 --lr-dec 3e-4
```
跑过 **1 次 eval** push autolog 即可(这轮信息密度够,不用等两次)。
