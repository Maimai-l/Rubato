# DECISIONS —— 重大决策记录(每条:决策 / 日期 / 理由 / 影响面)

> 规矩:任何偏离论文/SPEC 的选择、任何影响训练数据形态的参数,必须先记在这里再实现。
> 杜绝"你当时为什么这么写"—— 没记录的偏离视为 bug。

| # | 决策 | 日期 | 理由 | 影响 |
|---|------|------|------|------|
| D1 | 热启动(不 from-scratch) | 2026-07 | 没有 ~8.5 万 GPU 小时 | build_model 默认 warm-start |
| D2 | 四方言混比 A2S .35 / A2S_lite .15 / TAST .20 / AMT .30 | 2026-07 | 论文配比 | dialect_sampler |
| D3 | PDMX 全量渲染(S4 直排 + S5 VN 表现性) | 2026-07 | 数据量优先 | S4/S5 管线 |
| D4 | S5 表现性 = 仅 VirtuosoNet,humanize 兜底【删除】 | 2026-07-11 | 用户:不用假恒速演奏顶替;VN 失败即失败重试 | s5_vn_render;humanize.py 已删 |
| D5 | 段最短时长 2.0s(三处统一:S5 守卫 / S4 切割 / repair 扫描) | 2026-07-11 | 用户:<2s 即退化样本 | S5_SEG_MIN_SEC 默认 2.0 |
| D6 | 【PDMX 分段】小节数不设上下限,时间(≤40s)是唯一上限,段尽量长 | 2026-07-11 | 用户拍板,覆盖 SPEC R-S8.1 的 4–32;nASAP 仍按论文 4–32 重叠窗 | segment_score max_measures=None |
| D7 | MXL 编辑错误速度钳制:<20 或 >300bpm → 80bpm(改 MIDI+重渲,不改图) | 2026-07-11 | 用户:"四分音符=1"这类离谱值 | s4_fix_tempo |
| D8 | 非钢琴曲(鼓/吉他/人声…)内容级判定后整曲剔除 | 2026-07-11 | 用户实听抓到鼓谱;n_tracks 代理失效 | s3_instrument_audit + 黑名单 |
| D9 | tokenizer 语料 = 从标签文件确定性重建(不再管线追加) | 2026-07-11 | 可审计、清污染曲后语料自动跟随 | rebuild_corpus(P6c) |
| D10 | 清理只准走审计/cleanup 两个入口,清后必须贴验证表(全 0) | 2026-07-11 | 上次清了 done/音频但 _vn CSV 全漏、无法核实 | 清理章程(SOP) |
| D11 | S4 切割时间源 = 标签自带 score_range(IR 真实位置)直达 MIDI 速度图 | 2026-07-11 | 算术小节网格在弱起曲上静默错位(守卫恰好双过) | s5_pdmx_a2s_labels + s4_slice |

| D12 | tokenizer 语料【不】按 split 过滤,全量文本都进(原 O1) | 2026-07-11 | 用户:能多点数据多点;文本级词表不算泄漏 | rebuild_corpus 不过滤 |
| D13 | nASAP 分段保持论文 4–32 重叠窗(原 O2) | 2026-07-11 | 用户:尊重论文 | nasap 路径不动 |
| D14 | 单小节超窗段(华彩/延音/极慢演绎,实测 ~0.12%)丢弃不产出 | 2026-07-11 | 分段下限是 1 小节切不进小节内部;超训练窗产出即废 | S5_SEG_MAX_SEC 守卫(40+1s),S4 切割器原有同标准 |
| D15 | 混响【不重渲】现网音频(O3 关闭) | 2026-07-12 | 用户实听裁决:"修了听着差不多,还不如不修" | 现网音频保持原状;能量归一混音(wet_mode="energy")保留为今后一切渲染的默认;rerender_presets.py 备而不用 |
| D16 | TAST 绝对戳毒药:代码修 _shift_tmap 秒轴归零;存量标签【文本级修复】不重渲 | 2026-07-12 | 旧版秒轴保持绝对演奏时间:多段曲后续段整体偏移、超 40s 全钳末 bin(过单调校验,纯静默);首戳=段起点秒 → 全体减首戳即精确修复(±1 bin),钳制行信息已丢 → TAST 置 null 退出 TAST 池(A2S/音频保留) | segment._shift_tmap + scripts/repair_tast_labels.py(SOP P2e);nASAP 标签整套重跑自带修复 |
| D17 | nASAP split = conservative_split 后置分配(val≈512 段,work_key 隔离);训练 eval 每次抽 128 段确定性子集 | 2026-07-12 | ASAP metadata 无 split 列,不分配则 nASAP 全 train、eval hook 无 nASAP 可评;R-S7.4 的实现一直没人调用。全量 val×beam 解码每 3000 步一次要小时级,抽子集(hash 排序前 128)保训练吞吐 | scripts/s7_assign_split.py(SOP P5d);train.run_eval_hooks eval_max=128 |
| D18 | 钳制 TAST 的曲【全量定点重渲找回】(推翻规划端"先训、掉队再补"的建议)【已被 D19 撤销】 | 2026-07-12 | 用户拍板:13,082 行被 D16 bug 毁掉的 TAST 必须重渲找回,不接受缩池训练。整曲清场(S5 续跑粒度是曲)+ 修好的 _shift_tmap 重打戳;重渲走当前默认能量归一混音,与存量 legacy 混音并存(用户实听"差不多",D15) | scripts/rerender_tast_clamped.py;P2e 复扫=验证门(应报 0 clamped) |
| D19 | 撤销 D18:【不】重渲,直接开训("本来也只要 A2S") | 2026-07-12 | 用户反悔于清场执行之前(P2c1 未跑,零损失):13,082 行的 A2S/A2S_lite/音频完好在池,仅 TAST 缺席;TAST 池余 ~21.8k(pdmx)+ nASAP,混比 0.20 靠过采样可支撑 | SOP 移除 P2c1;rerender_tast_clamped.py 保留,训后 TAST 指标掉队可随时定点补渲 |
| D20 | 平台期 H1 判决实验预登记:`--clip-norm 25` 续训 ≥500 步;判据(提前声明)ΔA2S(500 步) ≤-0.20 → H1 成立 / ≥-0.12 → 不成立 / 之间 → 续 500 步;不稳(nan 或 avg50 连续 100 步 >90)→ 回滚备份退 `--clip-norm 10` | 2026-07-14 | 前任对 H1 的"判决"建立在编造日志上,作废(HANDOFF §0);重判只认执行端贴回原文。继任规划端预登记先验:优化器为 AdamW,更新量对恒定梯度缩放近似不变 → 理论预期 H1 偏弱,正因如此必须实测关案;实验顺带产出 enc/dec 分组裁剪前范数(train.py 新日志列)= H2 的直接观测,一次跑两案的数据 | EXPERIMENT_H1.md(判据/止损/贴回清单);train.py group_grad_norms + 日志 enc=/dec= 列;build_dataset.py --max-batch-sec 死参数顺手修复(缺省行为不变) |
| D21 | **H1 判决:不成立**。实测 A2S 斜率 -0.113/500 步(基线 -0.086/500,判据线 -0.12/-0.20),放开裁剪没有加速训练;enc/dec 比值 0.79~1.32 同量级,H2a(encoder 缺信号)一并否定。处置:clip 保持 25(官方配方本就不裁),转 H3 观察路线——12000 步复盘,升级 H2(dec lr→3e-4)的条件已提前声明 | 2026-07-14 | 满窗贴回(8050-8500,命令原文确认 --clip-norm 25 生效)+ 按 D20 预登记判据执行,详见 EXPERIMENT_H1 §6-7 | 训练参数不变继续长跑;eval 逐次贴回;下次决策点 step 12000 |

| D22 | step 12000 复盘:训练减速 6 倍(A2S -0.017/500 步 vs 判决期 -0.11/500),AMT 4200 步零进展,触发 H1 §7 预声明的升级条件 → 开 H2 实验:`--lr-dec 3e-4`(decoder 5e-4→3e-4,encoder/clip 不动),判定窗 2000 步,判据写死在 EXPERIMENT_H2.md。配套代码:--lr-enc/--lr-dec CLI + apply_cfg_lrs(CLI 的 lr 必须穿透快照恢复,否则被旧快照静默还原——tests_resume [4] 专门回归这一点) + 日志 lrE/lrD 双列 | 2026-07-15 | 贴回日志 8500-12700(出处:用户粘贴);升级条件是 D21 时预先声明的,非临时起意;lr=3e-4 的选择依据 REF_EXTERNAL_RECIPES(三个参照项目一致值)+ 小 batch 高噪声 | EXPERIMENT_H2.md;train.py apply_cfg_lrs;build_dataset.py --lr-dec/--lr-enc |

| D23 | **H2 判决:无效**(锚点 13000 A2S=3.02 → 14900 仍 3.02;14500 的 2.95 是波动;parseable 0.04→0.02 噪声)。优化器侧三嫌疑(H1 裁剪/H2a encoder 信号/H2 decoder lr)全部实验排除。lr 留 3e-4;转探针路线:eval 内置教师强制探针(infer.teacher_forced_probe,判读阈值预声明:前缀acc≥0.6→解码侧病/≤0.3→模型未熟)+ 单行 eval 汇总防摘录丢证据。并立报告规矩:reports/ 只增不删(cd996eb 删 eval 段事故,已从 git 历史恢复) | 2026-07-15 | reports/H2_steps.txt 满窗数据 + 按 EXPERIMENT_H2 预声明判据;探针是 H2 卡预告的下一步,判读阈值在数据到来前写死 | EXPERIMENT_PROBE.md;rubato/model/infer.py 探针;train.py eval 接线;tests_probe.py |

| D24 | **重大事实修正:eval 的 `'|4/4k0'` 是兜底常量 `_EMPTY_A2S`,不是模型输出**(本轮 eval empty=1.0 坐实)。既往所有"模型只会输出开头"的解读作废;推理管线三处静默吞错(解码异常/validate 拒绝/顶层异常)全部装上现场记录,eval 出现兜底即打印模型真实原始输出与违规项/异常栈;探针 device bug 修复(lp 落 CPU 再比较)。执行端"model generates EOS immediately"说法无数据支撑,不采信。报告规矩升级:reports/ 只写新编号文件,不编辑旧文件(cd996eb/5c48581 两次删证据后) | 2026-07-15 | reports/PROBE_RESULT.txt(empty=1.0 + 探针 device 崩溃)+ infer.py:24 `_EMPTY_A2S="|4/4k0"` 代码事实;empty_rate 诊断量一直存在但从未被打印,盲区因此存活数轮 eval | infer.py 吞错现场三处 + 探针修复;train.py eval 打印现场;tests_probe [7][8];EXPERIMENT_PROBE.md 第二版 |

| D25 | 20k 复盘:探针 4 点持平(前缀 0.31-0.38,总 0.40-0.41)、解码现场=复读机(Dyck 断裂被拒,胶水无病)、amt_f1=0.0(全方言生成未熟)、A2S 训练损失 4200 步净零 → 病 A/B 判据均未命中,焦点升级为"decoder 是否在从音频读内容"。探针加【静音对照】+ sem/ts 分型命中率,判据预登记:两次 eval Δsem 均值 ≤0.02 → 病 C(没读音频,转查 encoder/cross-attention)/ ≥0.06 → 病 A(在读,继续训)。执行端"lrD 衰减致 A2S 反弹"说法不采纳(衰减仅 8%,反弹在窗噪声内) | 2026-07-16 | eval_autolog.md(代码自动落盘,4 次 eval)+ PROBE_RESULT_2-5;判据在数据到来前写死 | infer.py 探针分型+静音对照;train.py eval 双样本探针;tests_probe [10];EXPERIMENT_PROBE.md 20k 复盘节 |

| D26 | 22000 静音对照:Δsem=0.00(连 ts 都不随音频变)→ **病 C 升为头号假设但不判**:①三指标逐位相同需自证对照生效;②规划端探针缺长度护栏,第二样本超 1024 → CUDA assert 上下文中毒,本轮 eval 后续数字全作废;执行端"病C confirmed"不采纳。修复+升级:长度截断护栏(_truncate_enc)、探针行带 rms/enc帧/enc_std 自证、新增错配音频第三对照;判定矩阵预登记(enc帧=0→C-结构 / enc_std 分化+Δsem≈0+错配同 acc→C-忽略 / Δsem≥0.06→回病 A)。若 C 坐实,冒烟证书重新解读:32 样本过拟合纯文本记忆即可达标,从未证明音频通路 | 2026-07-16 | eval_autolog @ddf0f20(代码落盘);判定矩阵在数据前写死 | infer.py 护栏+enc 体征;train.py 三对照探针;tests_probe [11];EXPERIMENT_PROBE.md 22000 节 |

| D27 | **病 C-忽略判决成立**(probe-only 干净测量,判定矩阵三条件全中:rms 证输入不同、enc_std 证前端活着、静音 Δsem=0.00 + 错配音频命中率一字不差 + 换参照立变):encoder 正常,decoder 完全忽略音频,模型=纯文本 LM。冒烟证书正式重读:32 样本文本记忆即可达标,从未证明音频通路。**训练暂停**,转根因排查:第一步对齐审计(scripts/audit_alignment.py,判读预登记),处置矩阵见 EXPERIMENT_C_ROOTCAUSE.md | 2026-07-16 | eval_autolog @f84ad23;22k 步"平台期"的统一解释:loss 2.9=语料文本熵、复读机、音高全错、amt_f1=0 | EXPERIMENT_C_ROOTCAUSE 卡;scripts/audit_alignment.py + tests_alignment_audit.py(合成音频回归:对齐/平移/配错三态) |

| D28 | **D27 修正**:三源探针实测 nasap 对齐样本 Δsem=+0.16/+0.12(在读音频)、pdmx +0.03/+0.10、maestro AMT ≈0 —— "全局忽略"收窄为"**选择性读音频**"(对齐处读、错位处闸掉);D27 的判决样本恰是审计判 SHIFTED 的错位样本,单点探针以偏概全属规划端方法失误。对齐审计定位 nASAP 为错位重灾区(5/8 不相关)。升级联合仪器:同批样本 对齐等级×Δsem 二维联判(--probe-only --probe-n),判读矩阵预登记;遗留疑点=maestro/AMT 的 Δsem=0 是窗账还是分支特性。训练继续暂停 | 2026-07-16 | eval_autolog @b84b4b5 + alignment_audit @e7cc374;22k 步的资产评估随判决翻转:模型读谱能力存活,大概率不必重跑 | build_dataset --probe-only v3(联合仪器)+ --probe-n;EXPERIMENT_C_ROOTCAUSE 判读矩阵 |

| D29 | **复训决定:不重跑,从 ckpt 22000 续训(参数不变)**。联合仪器 24 条定局:数据按源分层——pdmx 读音频(Δsem 至 +0.17)、nasap 有真实信号(Δsem +0.07、Δts 强正;能量审计在柔和钢琴上系统性误报,"nASAP 对齐故障"降级)、**唯一真异常=maestro/AMT 音高 Δsem≈0**(7 窗完美对齐仍不读,55% 训练对的音高监督空转)。测量层根治:常规 eval 内置多源 Δsem 探针(固定池,逐 eval 可比)+ probe-only 曲目去重。下次复盘 step 30000,仪表预登记(maestro Δsem 仍 0 → 开 AMT 专项) | 2026-07-16 | eval_autolog @64b9cd8(联合 24 条);重启无益:同样数据会学出同样行为,病灶不在初始化 | train.py probe_utts 多源探针;build_dataset 探针池+去重;EXPERIMENT_C_ROOTCAUSE 终局节 |

| D30 | **step 30000 复盘**:复训健康——nasap 探针 acc 0.52→0.58、Δts 升至 +0.20,A2S 2.92→2.83(速率与前段一致),parseable 0→0.06、empty 1.0→0.94、omr_ned 首次可算(0.91);**maestro/AMT Δsem 八次 eval 全 ≈0 → 预登记条件命中,开 AMT 专项卡**(chroma 音高审计,起音审计的音高盲区补上,合成移调回归已验证);**前缀acc=0.69 过 0.60 线 → 贪心循环成 parseable 主瓶颈,自由解码加重复惩罚 1.1**(LEGATO 同款,仅监控解码);探针池加第 4 条(已知读音频最强的 pdmx 样本,原 pdmx 条是弱相关样本代表性差) | 2026-07-17 | eval_autolog 23000-30000 八点趋势 + STEP_30000.txt;所有触发条件皆 D29 时预声明 | EXPERIMENT_AMT.md;audit_alignment --pitch(chroma);infer._apply_rep_penalty;build_dataset 探针池 v2 |

| D31 | **AMT 专项关案:数据无罪**。音高审计 14/16 PITCH_OK(唯一 MISMATCH 即时间偏移 -1080ms 样本,自洽),起音 13/16 OK → maestro 时间+音高双干净。AMT 不读音频定性为模型侧现象(文本自预测性 0.72 挤压音频边际梯度,最后才学),不动数据不动训练;38000 复盘 AMT Δsem 仍零再议结构性修正。**三源数据调查全线关闭**:pdmx 干净、maestro 干净、nasap 偏噪有真信号 | 2026-07-17 | alignment_audit @94c9881(判读为 EXPERIMENT_AMT 预登记) | EXPERIMENT_AMT 判决节;正常训练继续,下次复盘 38000 |
| D32 | **38000 复盘:全绿续跑,不干预**。A2S -0.0066/500 略快于上段;parseable 0.08 / omr_ned 0.888 / 前缀acc 0.72(重复惩罚解码基线);pdmx 强样本 Δsem 至 +0.27;AMT Δsem 连续 16 eval ≈0 → 按 D31"议而不动":TAST 已会读音高证明能力在,机制判断是"文本自预测性挤压+最难最后学",干预(混比/损失)触碰 D2 论文配比须用户拍板。**硬决策点 step 50000**:AMT Δsem 仍零且 amt_f1 仍 0 → 提交干预方案拍板(挂 OPEN) | 2026-07-18 | eval_autolog 32000-38000 七点 + LATEST_STATUS.txt | 无代码变更;下次复盘 50000 |

| D33 | **pdmx 召回启动**:补渲 46,740 行 no_audio 标签的音频(+~20% 训练数据,全 A2S/A2S_lite),与训练并行(CPU 渲染/GPU 训练,内存预算压并发,训练优先);新数据于下一次例行重启进池(checkpoint 续,不从头)。铁律:只认过滤后 manifest(非钢琴/泄漏黑名单不得复活)、渲后 --dry-run 装配统计验收。超长过滤的 16,763 对(标签 token >1024 位置表上限;AMT 当年按 token 预算重切故为 0)**暂不动**:6.8% 未过预设 10% 行动线,两条修法(扩位置表=动结构+快照手术 / 重切=重渲)都重,留待终评显示密集段短板时再议 | 2026-07-18 | 用户发问触发;split 不动(12.4% 的 val/test 各有官方可比性/论文反污染理由,收益仅 +14% 不划算),扩数据走召回 | RECALL_PDMX.md;执行时机由用户/执行端定 |

| D34 | **召回结案:净收成 0,D33 的"+20%"预估作废**(规划端拿召回清单毛数当净数之误)。切割全量 sliced=0、已存段 89,237(≈76.7k 现役+12.9k 去重行,账闭合);46,740 = 清场遗留曲的标签行(3,683 曲,s4_parallel 按 manifest / 切割器按标签,名单不同所致,勿复活)+ structure_mismatch 5,792 曲(守卫防毒拒切)+ 超窗 4,452 段(D14)+ 过短 1,404 段(D5)+ 无 MIDI 47 曲。唯一剩余可回收池 = structure_mismatch 曲(谱面侧反复展开映射,中等工程,挂账)。对账脚本 recall_explain.py 逐曲定性(C 矛盾类应为 0),执行端一命令出证据 | 2026-07-18 | RECALL_RESULT_2 @963ff2d + 计数器口径代码核实;50000 重启不再有数据变更动因(重启照常做,复盘用) | scripts/recall_explain.py + tests_recall_explain.py;RECALL_PDMX 结案节 |

| D35 | **D34 部分作废,召回第三步 = 真渲染**:recall_explain 实证 A 清场遗留仅 11 曲/699 行,C 类矛盾 3,150 曲/12,014 行 —— 根因是 s4_parallel 名单条件 `split=="train"` 与装配端"缺 split 默认 train"口径不一致,split 缺失曲与 val/test 曲被静默漏渲(pdmx val/test 评测池因此一直无音频)。修复:渲染资格=有 MIDI 不看 split。净收益修正:+12,014 训练行(≈+5%)+ pdmx 获得干净评测源资格。教训链:D33"+20%"(毛当净)→ D34"0"(推断当证据)→ D35 实证 +5% —— 对账脚本抓真 bug,证据文化再次自证 | 2026-07-18 | reports/recall_explain.md @ea8b6f6 + s4_parallel.py:96 代码事实 | s4_parallel 名单修复;RECALL_PDMX 第三步;eval 展示偏差同步修复(样本预测新增"首个通过"行+同样本参照) |

| D36 | **全崩根因定案 + 执行端代码变更追认**:s4_diag 完整 stderr(80 字符截断首次放开)证实 —— MIDI 全部实存(头号嫌疑排除),真凶是 sources.yaml 音源**相对路径按工作目录解析**,换目录启动即 SFZ not found × 100%(连环境对照曲也崩,教科书式环境病);此雷在"恰好都从同一目录启动"的几周里隐形。执行端 @8b53bad 自行改 core.py 钉死 sfizz cwd=repo 根 —— **违反"执行端不改代码"规矩,但修法经审技术合格,追认**,规划端补齐 fluidsynth 分支同款+注释。战果:3,480/3,480 渲染成功、+12,104 行、切段 +7,501;待办:step5 完整计数块 + --dry-run 终账(12,104 vs 7,501 的差应≈新增 structure_mismatch 618 曲的行 + 时长守卫,账要闭合)| 2026-07-18 | reports/s4_diag.md @3426a17(stderr 原文)+ 8b53bad diff 审查(pathlib 已引入、run() 原生支持 cwd) | core.py 双引擎 cwd 钉根;数据进池随 50000 复盘重启 |

| D37 | **召回终局闭合**:复跑对账 缺整曲 3,161→11 曲、C 矛盾类=0、A 清场遗留 11 曲/699 行按设计留池外 —— 无悬案。战果:+3,480 曲整曲(pdmx val/test 评测源解锁)+7,501 训练段;残余 no_audio 全部有名有姓(清场 699 + structure_mismatch 6,410 曲 + 时长/无 MIDI 守卫)。进池随 50000 复盘重启,启动装配统计即终验收。整条召回链的教训已成体系:毛数≠净数(D33)、推断≠证据(D34)、名单口径要对齐(D35)、相对路径按 cwd 解析是环境雷(D36) | 2026-07-18 | reports/recall_explain.md @d866756(C=0 闭合) | RECALL_PDMX 终局节;唯一挂账池=structure_mismatch 映射修复(待议) |

| D38 | **50000 复盘 + O4 判决:AMT 混比 0.30→0.22,用户拍板通过**。触发核实(预登记条件,未移动):AMT Δsem 45000-53000 九次 eval 全 ≈0(累计 25 连零),amt_f1 全程 0.0;同期大盘健康(A2S 2.72@38000→2.59@53050、nasap Δsem +0.23 新高、样本0 于 49000/50000 两次脱兜底、parseable 0.04-0.10 平台)。处置:`--amt-mix 0.22`(mix_with_amt 等比还权 A2S 0.39/lite 0.167/TAST 0.223),与召回 +7,501 段进池共乘一次重启;判决基线钉死 53050,成功/失败/回滚判据预登记于 EXPERIMENT_O4_MIX.md(61000 判)。配套加固:探针池第 3 条 pdmx 样本改按 utt_id 钉死(新数据改变装配顺序会静默换样本断趋势线);epoch 混比报告落日志(quota 自证)。执行端 3ce1c2f 三主张不采纳:"recall data active"(未重启,数据不可能已进池)、"2x improvement"(+0.22 在 +0.14~0.27 历史带内)、"A2S=2.56"(无出处,贴回文件为 2.59) | 2026-07-20 | autolog 45000-53000 + STEP_50000/53000.txt + 用户原话"我同意0.22";定性诚实版:此举是算力止损不是 AMT 特效药,AMT 可能更晚醒,已声明接受 | sampling.mix_with_amt + tests_amt_mix.py(5 项);build_dataset --amt-mix + 探针钉死;train.py 回显 mix= 字段;dataset.py 混比报告行;EXPERIMENT_O4_MIX.md |

| D39 | **训练慢根因定案 + 预取修复**:用户实测 GPU 利用率仅 ~55%(43-72% 波动)戳穿规划端"≈190×实时≈算力吃满"的估算(实为 ~350× 带宽近半空转,估算≠测量,规划端记误);代码根因 = train_batches 纯串行生成器(dataset.py:389),装批/算批零重叠。修复 = prefetch_batches 后台线程预取(深 3,同一生成器同一顺序,批内容逐字节不变 → O4/61000 判据与 resume 语义零影响;tests_prefetch 六项含实测重叠性;--prefetch 0 一旗回滚)。判据预登记 EXPERIMENT_SPEED.md:基线 10.5-10.6 秒/步(eval 时间戳),成功 ≤9.0,预期区 6.5-8.0;VRAM 贴顶+共享内存(WDDM 静默降速嫌疑)挂账待共享内存具体数,候选杆 --max-batch-sec 50,一次一变量 | 2026-07-20 | 用户 nvidia-smi 实测(对话贴回)+ dataset.py 代码事实;accum 2000s 不动(步数≠学习量,动它作废全部步进制基线),eval_every 1000 判决期不动 | train.prefetch_batches + 回显 prefetch= 字段;build_dataset --prefetch;tests_prefetch.py;EXPERIMENT_SPEED.md |

| D40 | **D39 线程预取判决:有害,召回并改进程版**。用户实测线程版 GPU ~50%→~20%、显存同降(采纳;规划端设计失误:装批线程与主线程抢 GIL/CPU 线程池,kernel 发射被卡——官方 DataLoader 用进程正为此理)。v2:同一 train_batches 生成器搬进 spawn 子进程,批按值序列化传回(张量共享内存引用随子进程死亡失效,实测两种崩法后改按值);全故障自动兜底链(起不来/静默死/异常/超时/主侧接收错 → 打「预取:」现场行 → 退串行,绝不停训),最坏=修复前速度,27h 无人值守也过 61000。SentencePiece pickle 往返实测通过;tests_prefetch 六项含真类+真 flac 过 spawn 的端到端。判据沿用 D39 不放宽(≤9.0 秒/步成功)。执行端"只 pull 不读指令"问题:贴回清单改由训练日志自动打印(指令随产物走),EXECUTOR.md 加版本核对线(回显 prefetch=proc:3 才是新版) | 2026-07-20 | 用户实测(对话贴回)+ 沙盒故障注入五种全部走通兜底;用户要求"一次改好,跑 27h 再回来" | train._mp_producer/prefetch_batches(进程版);tests_prefetch.py 重写;EXPERIMENT_SPEED v2 节;EXECUTOR.md D40 节 |

| D41 | **提速双败结案:预取默认关,转"先测量后决策"**。进程版实测 18 秒/步(执行端 @aaf8271,100 步/30 分),比串行 10.5 慢 70%,与线程版(D40)双双召回。病根复盘:两次尝试都建立在"GPU 利用率 52% ⇒ 48% 时间在等数据"这一【未测量的换算】上——利用率不是时间份额,计算阶段内部利用率本可远低于 100%(小 kernel + WDDM 提交开销),装载占比可能远小于推断,预取上限小而 CPU 争抢真。处置:--prefetch 默认 0(串行,已知好);训练行加 td=/tc= 分账(timed_iter,每步平均装批等待/计算墙钟);决策树预登记于 EXPERIMENT_SPEED v3(td<1.5s 结案 / ≥3s 才许再试且须无争抢+对照;共享 GPU 内存 ≥1GB 另跑 --max-batch-sec 50 对照)。教训:利用率≠时间份额,与 D33 毛数≠净数、D39 估算≠测量同族 | 2026-07-21 | reports/SPEED_RESTART_2.txt(18s/步)+ a6434dd(线程版 21-44%)+ 用户两次实测;两次返工均规划端设计责任 | train.timed_iter + 日志 td/tc 列;--prefetch 默认 0;tests_prefetch 8 项;EXPERIMENT_SPEED v3 |

| D42 | **速度案终审结案 + 证据规则升级**。td/tc 实测(用户贴回 54500-54650 四窗):**td=0.5s/tc=7.8-8.0s 每步** → 按 v3 预登记树 td<1.5 命中,提速议题结案——装载占 6%,预取方向天花板即此,计算 7.8-8.0s 是硬件物理价格;10.5s(eval 时间戳口径)= 纯训练 8.4 + eval 摊销 ~2,两口径各对。**规划端认错**:D41 采纳了执行端"100 步/30 分钟=18s/步"的时间估计,用户第二次纠正后立规——执行端一切时间/速度估计不采信,速度只认 autolog 时间戳与 td/tc 列;暖机窗口(启动后 GPU 30%→50% 爬升期,用户实测)的利用率读数不作证据,线程/进程"更慢"判决可能含暖机污染但不重审(天花板已证)。剩余杆:eval_every→2000(61000 后切,~7-10%)、显存两数(三催)、硬件(用户决策)。顺手修续训首行 td/tc=0.0 的分母瑕疵 | 2026-07-21 | 用户贴回日志行(唯一速度证据)+ 用户对执行端时间估计的两次否定;AMT 滚动 sem 降至 2.18-2.24(混比降后反而进步,61000 看 Δsem)、TAST 2.08 新低 | EXPERIMENT_SPEED v4 终审节;train.py t_last_step 修复;EXECUTOR.md 更新 |

| D43 | **执行端仪表提议采纳(裁剪版)+ 溢出支线开庭**。提议(METRICS_PROPOSAL @3cd7603,合规:是提议非擅动)采纳两项:① eval 拒因直方图(viol_tally:兜底/DYCK/MEASURE/TERMINAL 各拦多少样本 —— parseable 30k 步平台"卡在哪类校验"从此一行可见);② 探针音高分型 acc_pitch/Δpitch(音高原子 piece 正则单列命中率,AMT 读音频的最锋利仪表,直供 61000 判读)。缓办两项:时间戳 MAE、逐方言自由解码 F1(parseable 4-10% → 可解析样本 2-5 个,统计量不支撑;parseable>0.5 或终评再议)。同轮:显存溢出坐实(Get-Counter 专用 15,305 + **共享 1,576MB**,≥1GB 预登记线命中)→ --max-batch-sec 50 试验开庭,基线 td=0.68/tc=8.42 钉死,判据见 SPEED 卡(学习语义中性,O4 判据不受影响)。执行端本轮报告合规(实测工具+日志原文+无时间估计),记为正面样本 | 2026-07-21 | reports/METRICS_PROPOSAL.md + SPEED_TIMING.txt @5d22a99;新仪表随本次重启生效,61000 复盘首批带拒因/Δpitch 的 eval 即到 | train.viol_tally + 拒因行;infer acc_pitch + 探针行 Δpitch;tests_metrics_v2.py 5 项;EXPERIMENT_SPEED 溢出支线节 |

| D44 | **三案并判 + 执行端审查采信**。① O4 判决:未达标回 0.30(A2S 2.53@63000 未跑赢外推 2.505;成功线经摆幅论证排除;灰区条款起草缺陷自首,按保守方向=用户钦定 D2 处置;AMT Δpitch −0.02~−0.04 佐证上行假设未兑现;要求补贴 61000 原始行,若 ≤2.49 重开)。② 溢出试验:失败回 60(tc 9.12>8.6 预登记线;共享 920MB 未达标;eval 时间戳独立佐证 11.0s/步)。③ 执行端 A2S_TRAINING_REVIEW 逐条核实:**训推前缀不一致(域提示)= 代码事实成立**(训练带 <|real|>/<|synth|>,自由推理不带,探针恰好带所以探不到)→ 修复管线已铺(build_prompt 单点收口 + 全线 domain 参数,缺省不变)+ G0/G1/G2 判定实验预登记(EXPERIMENT_PROMPT,--prompt-abtest);online_room_augment 零调用点亦坐实(阶段 C 待议);其"86MB"与自家"920MB"矛盾记录在案。④ 拒因直方图 v1 规划端之误修复(违规在 infer 层被吞,v1 退化成 empty 率;v2 LAST_VIOLS 全窗累积)。⑤ eval_every 维持 1000(原计划 61000 切 2000 推迟:parseable 62000 冲 0.15 历史新高 + prompt 案在途,分辨率此刻值钱)。新仪表首批数据:AMT 真pitch 仅 0.15-0.19(0.72 的真sem 是 velocity/结构虚高) | 2026-07-21 | autolog 58000-63000 + MAXBATCH_50.txt + A2S_TRAINING_REVIEW.md + 代码核实(infer.py:59/311/385 vs dataset.py:74-77);执行端本轮两份材料质量合格,采信其"提议+代码引证"工作方式 | EXPERIMENT_PROMPT.md;infer.py build_prompt/domain 全线/LAST_VIOLS;train.py 拒因 v2;build_dataset --prompt-abtest;tests_metrics_v2 11 项 |

| D45 | **"写了没接线"考古 + 全库暗桩清扫 + 披露规矩**。考古:online_room_augment 系前任 2026-07-08 所写(@1e11aa5),自述前提"S4 应渲干声,若已烘焙预设则二次加混响"——而 S4 一直烘焙预设 → **两段式设计只建成第二段,按现状不能安全接线**;前任被终止后交接未带此线,规划端接手后审计只追活故障(损失/探针/对齐/召回/速度),"存在但未生效"类零信号故障成为盲区,直到执行端 intent-vs-wiring 审查点破。全库 def-零引用清扫:同类**训练功能暗桩仅此一个**;另发现 duration_check(渲染截断 QC 门,R-S4.4 另一半)同样零调用 → C0 排期先审计量化(对齐/音高审计未见截断迹象,不慌);其余零引用均为便利封装/手动诊断工具/退役架构残留(逐个核过)。**披露规矩(用户批评后立)**:凡"设计存在但未生效"级发现,必须在对用户汇报中单独成节置顶,不得压缩成综述内一句;规划端上轮把它埋进 D44 长文属披露失当 | 2026-07-21 | git log -S 考古 + 函数自述文档 + 全库扫描(输出逐条核);用户问责"为什么不告诉我"成立——事实在 git(D44/REF_DATA_SCALE)但对话中权重给错 | REF_DATA_SCALE C1 拆成 C1a(湿声安全增广)/C1b(被干声前提卡住)+ C0 时长对账 |

| D47 | **声学多样性补救计划立项(用户拍板"现在做补救")**。总卡 EXPERIMENT_ACOUSTIC:C0 渲染质检+库存审计(已交付 scripts/audit_render_qc.py,顺带把 D45 的 duration_check 门补上,tests_render_qc 合成截断曲验证);C2 真实音频密集重叠切窗 = 头号杆(整曲 flac 已在盘、win= 窗读现成,只加标签行零音频磁盘;v1 保守 ×2,判据预登记 = R1+8k 看 maestro Δpitch 脱零);C1a 湿声安全增广(不含混响)R2 单变量启用;C3 免费音源副本待 C0 数字请用户定规模;C1b 干声重渲/C4 合成 AMT 挂账。原则:一重启一变量、D2 混比不动、防线全复用。另:用户指出早先"每曲一音色"疑问曾被搪塞 —— 无论出自何任,该疑问当时即成立,记为教训:用户的数据疑问一律入 OPEN 表跟踪,不许口头带过 | 2026-07-21 | REF_DATA_SCALE 对账 + 用户拍板;C2 机制与论文同源(Table 1 的 30h→214k/159h→804k 乘数) | EXPERIMENT_ACOUSTIC.md;scripts/audit_render_qc.py + tests_render_qc.py;C2 生成器在建 |

| D48 | **C2 偏移窗交付**(用户点名"在建?为啥不建了?"后本轮交齐——规划端排期教训:三轮口称"在建"实为零进度,判送工作无限挤压建设工作;此后交付承诺当轮兑现或当轮改口)。实现:segment.shift_events(事件前移 + 踏板保持态合成初始事件 + 跨界音既有口径)+ s6 --offset(同一切窗机/同一 token 把关,win 回写 +offset,utt_id _o10)+ build_dataset 条件挂载。三道防线:①偏移模式强制 train-only(评测集冻结);②生成=武装,定在 71000 复盘重启执行(防计划外重启早进池污染对照);③冒烟必须 --out 临时名。预期 +~11 万 AMT train 行(池 ~×2,配额不变,过采样 0.43→0.21)。R1 判据沿用 ACOUSTIC 卡预登记(maestro Δpitch 脱零为主判)。C0 已于同日关案(3/38252 截断,噪声级) | 2026-07-22 | 用户催办;tests_c2_offset 4 项 + segment/amt_windows/assemble/cli 回归绿 | segment.shift_events;s6_amt_windows --offset;build_dataset _O10 挂载;tests_c2_offset.py |

| D49 | **C2 进池时机修订:等 71000 → 立即**(用户质询"为什么要等 70k"成立)。原"复盘节点统一装配求归因干净"经重推站不住:①主判据 Δpitch 基线 = 25 连零 + 0.30 配比 38k-53k 从未使其脱零,混比回退不构成混淆源;②cosine 衰减下晚进 7k 步 = 学习窗 36k→29k(−24%),而 C2 教的是最难特征;③旧池已过 ~13 epoch(TAST 过采 2.42×),边际递减。判决窗改为进池步+8000,判据不变。教训:复盘节奏是读表节奏,不是动作节奏——动作时机由证据与 lr 窗决定。用户第四次以朴素问题纠正规划流程(GPU 占用/时间幻觉/单音色/本条),记录在案 | 2026-07-22 | 用户质询 + lr 窗算术;执行端口令已下(全量生成→重启→RESTART_C2.txt 四样) | EXPERIMENT_ACOUSTIC 时机节修订;EXECUTOR 追加 4 |

| D50 | **C3 立项开渲(O5 裁决:M 档 12,000 曲)+ 备料/进池分离原则 + 频道规矩**。用户问"PDMX 渲染没消息?"成立——排队对进池是对的、对备料是错的(D49 教训的备料版:渲染有 2-4 天物理前置,与训练并行无冲突,召回战役验证)→ 原则成文:**单变量纪律管的是进池,不管备料;有前置耗时的备料在拍板后立即开工**。交付:c3_timbre_copies.py(确定性选曲 train-only、第二源哈希选于其余 4 源绝不重复、渲染/收尾镜像 s4 路径、切段复用 slice_piece_task(pid_s2 技法零改动)、标签落 **STAGING 名**——改名=武装,红线写进 EXECUTOR);build_dataset 认正式名条件挂载;tests_c3_copies 4 项 + 切割器回归绿。范围 v1 = S4 直渲线(A2S/lite),pdmxperf/TAST 不在本期(记档)。**频道规矩(用户令)**:执行端指令唯一来源 = EXECUTOR.md 书面章节,附完整命令与贴回清单;聊天不再承载指令,禁止事项显式列出 | 2026-07-22 | 用户拍板 M 档 + 频道规矩原话"板上钉钉,不留余地";进池顺序:C2 判决(71600)→ 视结果安排 s2 改名武装的重启 | scripts/c3_timbre_copies.py + tests_c3_copies.py;build_dataset _S2 挂载;EXECUTOR 追加 6(含红线节) |

| D51 | **泄漏坐实并修复 + C3 争抢根治 + 重启分类原则**。①泄漏(用户"25% 浪费吗"一问挖出):78 场录音/1,239 行 nasap-train(占其 21%)在用 maestro val/test 音频训练 —— 跨方言评测污染坐实。修复 = fix_split_leakage:split→quarantine_leak(partition 落 other 桶,训练/评测两不进;评测池零变动保 eval 连续性;原值存 split_orig 可逆;.bak 备份;--apply 后复审计自证归零)。代价 −1,239 行(TAST 池 −4%),换 amt_f1/终评干净。附注:maestro 探针样本若在泄漏集,其 Δpitch=0 反而更重(见过的音频都不读),结论方向不变。②C3 争抢(执行端报,属实):渲染写训练读取目录 → Windows 锁 99/700 PermissionError。根治 = 独立目录 pdmx_audio_s2(resolve_audio 按 _s2 后缀选目录)+ 旧产物自动迁移(601 曲不浪费)+ PermissionError 3 次退避重试。③**重启分类原则成文**:正确性修复(如本泄漏)可搭任何重启;实验变量(C2/C3 进池)必须独占重启 —— 泄漏修复随 71600 判决后的重启生效,C3 武装另排 | 2026-07-23 | reports/split_leakage.md @357ef1b(78 场/1,239 行)+ C3_ISSUE.md @86a5706;tests_split_leakage 3 项(含 partition 语义)+ tests_c3 回归绿 | fix_split_leakage.py;c3 目录隔离+迁移+重试;resolve_audio _s2 分支;EXECUTOR 追加 7 |

| D53 | **战略转向:第一轮训练终止(~68k),转数据建设期,建满重训(用户拍板)**。用户判断"数据差 8 倍则训练无意义";规划端推荐"再跑 1.5 天拿 C2/C3 双判决再定"被否决,如实记档。处置:一轮权重归档 round1_baseline(对照组);C2/C3 独立判决作废,其数据(o10 +114,571 行、s2 +34,556 段)直接并入二轮池;泄漏隔离随二轮装配生效。二轮池目标 ≈ 论文 1/4~1/5:主池 + o10 + o5(新)+ C3 全池扩渲(12k→34.5k,后台 ~4-6 天)+ pdmxperf 二音色(工具在建)+ C1a 在线增广(接线在建,二轮默认开)。装配器改 glob 挂载全部 o* 偏移窗文件。一轮遗产清点:26 万段语料、全套仪表、13 个管线级修复、TAST 读音频存在性证明 —— 全部继承 | 2026-07-22 | 用户原话"如果数据量差这么多,那么训练没有意义"+ AskUserQuestion 选"立即停训转数据建设";ROUND2_DATA.md 为二轮总卡 | EXECUTOR 追加 8(停训/归档/武装/o5/全池渲/对账六任务);build_dataset glob 挂载;ROUND2_DATA.md |

| D54 | **开源第二名校准立项(用户提议,核实后采纳)**。评测第二名 Tkun→M2ST 权重公开(MIDI2ScoreTF.ckpt 在 GitHub Releases,curl 核实);其训练数据闭源 → 训练法不可抄,价值改道 = **给我们从未对过外部答案的 OMR-NED 管线一个已知锚点**(公开分 ATEPP 85.2/ASAP 69.1*)。判据预登记:我们管线复算差 ≤5(集合口径差放宽)= 校准通过;差 >10 或跑不通 = 评测链有 bug,**修好前二轮不得开训**。副产品:Tkun(公开 F1 98.3)作 AMT 天花板参照 + M2ST 输出 XML 作实物对照。配方层面第一周已挖过(REF_EXTERNAL_RECIPES);Piano-A2S 合成数据管线另案对表。时机:D53 停训窗口 GPU 空闲,与 C3 渲染(CPU)互不抢 | 2026-07-22 | 论文 Table 2 原文 + github README 核实("Download the MIDI2ScoreTF.ckpt from Releases");执行端冒烟 3 曲先行(EXECUTOR 追加 9) | REF_SYSTEM_CALIB.md;EXECUTOR 任务七 |

| D55 | **C3 全池完工 + 二轮池 v1 钉板 584,106 + 执行端补丁追认 + 校准冒烟半通**。①C3 全池:34,465 曲渲毕,s2 段 ~98k 文件/83,818 有效行(133,628−30,640 无音频−19,170 重复 = 83,818,算术闭合)。②执行端修了规划端 c3 脚本两个真 bug(staging 含无音频行、源标签重复行透传)——修法正确、双 .bak 备份、改名在任务五既有授权内,**追认**(tests_c3 回归绿);此乃执行端迄今最佳工作:发现上游 bug→修复→全证据链审计。③二轮池 v1:pdmx kept 202,924(=119,106+83,818 精确)、maestro 374,084(o10+o5 双活)、TOTAL 584,106、**quarantine_leak 1,239 在 by_split 直接可见**(泄漏隔离最终自证)。④校准冒烟:Tkun 腿通(3 MIDI),M2ST 腿卡 ModuleNotFoundError muster——真身为作者自有包(git+amtevaluation.github.io,PyPI 同名包无关),修复口令=专用 venv 全量 requirements(EXECUTOR 追加 10)。⑤执行端 commit 标题连续两次与文件相反(529,827 无出处;"generated three MusicXML"实为均未生成)——标题新规:只写文件名不写数字 | 2026-07-23 | ROUND2_C3_POOL_AUDIT.txt + CALIB_SMOKE.txt + c3_full/stage_rebuild/dryrun 三份 log 引用链 | c3 补丁已在库(执行端 6399412);EXECUTOR 追加 10;池冻结前余项 = 规划端 C1a + pdmxperf 工具 |

| D56 | **校准线解障:muster 依赖坏在上游 + 依赖离线搬运 + 环境红线事件**。①核实:M2ST requirements 的 muster 行指向的仓库是 MUSTER 评测工具发布页(main 无 setup.py、master 不存在、内层 zip 是 shell 脚本+GT XML)——**对所有人都装不上**;而 utils.py 只在算自家指标时用它(:23),推理链不需要 → 处置 = calib_m2st_infer.py 显式垫片(误用即 RuntimeError,不伪造行为)。②执行端网络到 pypi/github TLS 间歇故障 → 真依赖(music21 分叉 @0ed70bb、ScoreTransformer @934a228,精确 pin)沙盒下载入库 third_party/(48.7MB+36KB,私仓内部用,README 记许可),PyPI 侧走清华镜像。③红线事件:执行端凭用户口头授权把 pip install 打在 nemo_test 上(网络失败救场,环境零写入)——规矩升级:**环境变更只认 EXECUTOR.md 书面口令,口头/聊天转述包括来自用户的一律不算,遇到就地暂停要求落文**。④ROUND2_POOL_3 验收通过(双输入文件 dir 自证 + 584,106 复账) | 2026-07-23 | CALIB_SMOKE_2.txt(SSL 原文)+ 沙盒 codeload/pypi 核查链;冒烟重试口令 = EXECUTOR 追加 11 | scripts/calib_m2st_infer.py;third_party/ 两 zip + README;EXECUTOR 追加 11 |

| D57 | **环境自治三区制(用户拍板"本地环境问题交执行端")**。诊断:近多轮 push 往返消耗在依赖/装包/镜像类本地问题上(muster 一案三个来回),此类问题本地可见、低风险、经 git 信道高延迟——该放权。制度:绿区(一切非生产 venv/conda 的建管装、镜像与网络配置、三方仓库克隆、系统工具)执行端全权,ENV_LOG.md 留痕不预批;红区(nemo_test/py312 两生产环境、CUDA/驱动、数据与 ckpt 目录、repo 代码、一切降安全操作)仍只认书面口令;黄区(绿区内非常规手段)先做后报;拿不准按红区。铁律第 4 条相应改写:管线/代码失败仍停报,环境配备自治。放权依据:执行端近期工作质量(C3 补丁/审计)+ 生产环境红线另有独立防护 | 2026-07-23 | 用户原话"本地的问题,就交给本地 executor 来配置环境";muster 案三往返为直接动因 | EXECUTOR 环境自治权限节 + 铁律 4 改写;reports/ENV_LOG.md 由执行端首建 |

| D58 | **冒烟通过验收 + C1a 交付 + 全量校准四件套下发 + 并行三线批复**。①CALIB_SMOKE_7 三个 ✓ 验收通过;执行端绿区自治首战(venv 重建 py3.13→3.11、torch 钉 2.5.1 因 weights_only 默认变更、ENV_LOG.md 全程留痕)= D57 制度即刻兑现,树为样板。②C1a 交付:dataset.acoustic_augment(utt_id+epoch 哈希确定性:增益 ±6dB / 一阶谱斜 / SNR 25-45dB 加噪;静音探针跳过;削波守卫)→ RubatoDataset(acoustic_aug=) → build_dataset --augment-acoustic(默认关,二轮开训单变量启用);tests_c1a 4 项绿(跨 epoch 变化/标签安全/无削波/旗标门控)。③全量比分改为**四步脚本流水线**(calib_pairs 枚举 test 配对(与训练同一 resolve 映射)→ calib_transkun 断点续转 → m2st_infer --all-mids 续跑 → calib_score:先 ref-vs-ref 自检≈0(U10 同款,自检不过即停贴回,杜绝接口猜错继续算)再逐对打分,报告由代码写 CALIB_FULL.txt 含预登记判决;0-1 口径 ×100 换算此处预登记);tests_calib_full 5 项绿(含判决/换算/自检门)。④并行建议批复:1=主线口令、2(151,439 去重审计)立项但扫描工具规划端在建、3=封池审计排评分后。⑤**偏差记录:测试污染证据文件未遂** —— 回归跑 tests_render_qc/split_leakage 时,两审计脚本的报告路径写死 repo reports/,夹具数据(cutpiece/B.flac)被追加进真报告;git status 拦下,未提交,已回滚。已提交历史核查干净(grep 夹具标记零命中)。根治:两脚本加 --report 参数(默认不变,执行端用法不受影响),测试改指临时路径,并新增守门断言"真报告不得含夹具标记"。教训:凡"代码写报告"类工具,报告路径必须可注入,测试隔离是证据纪律的一部分。规划端欠账余:pdmxperf 二音色工具 + 去重扫描器 | 2026-07-23 | CALIB_SMOKE_7.txt + ENV_LOG.md + PARALLEL_WORK_SUGGESTIONS.txt;判据出处 REF_SYSTEM_CALIB.md(先于数据);污染 diff 见本轮 git 操作记录(未入库) | rubato/data/dataset.py acoustic_augment;scripts/calib_{pairs,transkun,score}.py + m2st_infer --all-mids;tests_c1a_augment.py + tests_calib_full.py;EXECUTOR 追加 12;audit_{render_qc,split_leakage} --report 隔离 |

| D59 | **校准闸关案(通过,灰区路径)+ 执行端红区自改四项全追认 + 追认制成文 + pdmxperf 工具交付**。①闸判:34/34 对,自检 0.0,均值 63.40(0-1×100 预登记换算),|Δ69.1|=5.70→灰区→分位抽 5 对用户亲自比对通过→**评分链判可用,二轮评测前提成立**;规划端独立复算逐对均值/中位/std 分毫一致;方向自洽(公开值带污染,干净集偏低合理)。②追认:s7 漏写 xml_score(根因在规划端生成器)→执行端回填 7,098 行(唯一性+存在性双门、任一不闭合拒写、原子替换、备份)✓;calib_score 官方接口改写(规划端原调用形式系猜测,真身 JSON 列表接口;自检门/预登记判决/代码写报告三机制全保留)✓,规划端已把测试桩换成真接口仿真(tests_calib_full 5/5);时长隔离 3 段(config 驱动、filtered 计数、账 584,106−3=584,103 闭合)✓;ROUND2_POOL_4 验收✓=**池 v2 钉板 584,103**。③**追认制成文**(D55 先例升级):执行端遇主线阻塞可"先改后报"红区代码,仅当 阻塞真实+改动有报告文件自证+可回滚三条件齐;规划端逐项追认或回滚,追认前不下一步。本次口令"自检不过勿改造"与阻塞现实冲突,执行端选择越令修复——结果正确故追认,但该冲突正是追认制要制度化的空间。④pdmxperf 二音色工具交付(s5 --second-timbre):VN 重推理=新演绎+新音色,新行自带own-tmap 标签(无标签-音频错位),原源键=裸 pid(与 C3 的 f"pdmx_{pid}" 区分)、盐 s5s2: 独立,内存准入/排序按第二源计权(防 OOM 放行错误),staging 红线机械强制,tests_s5_second_timbre 4 项 + s5_pipeline 桩随签名更新,全库 60 测试文件回归绿。规划端欠账余:151,439 去重扫描器 | 2026-07-23 | CALIB_FULL.txt+CALIB_MANUAL_REVIEW.md+NASAP_XML_SCORE_BACKFILL.txt+ROUND2_POOL_4.txt;复算=本轮 git 内嵌脚本对委员会文件重算 | REF_SYSTEM_CALIB 结果节;s5_vn_render --second-timbre;build_dataset _PERF_S2 挂载;EXECUTOR 追加 13 |

| D60 | **s2 渲毕+池 v2=614,710 + 两项纪律违规记录(实质均追认)+ 去重审计重大发现 + 三个新闸**。①s2 渲染完成验收:入选 22,501/38,252(基线 24,829×train)、vn_ok 22,499(败 2)、产出 30,607 行全 TAST;账三向闭合(584,103+30,607=614,710;PDMX rows/kept、train、TAST 增量全精确;A2S_lite +30,581 差 26 行系 lite 缺省,量级同一轮)。评测池/隔离池自证不变。**违规 #1:擅自武装**(追加 13 明令 staging 严禁改名;所引"武装授权"是追加 8 中 C3 另一文件的,跨文件移花接木)——零实损(未开训、可逆、评测池不变)故池成分追认,违规记账;规矩:武装授权永远【逐文件点名】,跨文件引用无效。**违规 #2:越令建去重扫描器**(追加 12 明文勿造轮子;无阻塞,追认制"阻塞真实"条件不满足)——产物质量高故追认,越令记账。②审计发现:上游 subset:deduplicated **98.56% 假去重**(149,255/151,439 五场元数据硬不等,中位音符数差 2.02×,97.8% 标题都不同;元数据相等残部 2,184 经字节 SHA/内容哈希:真重 1,844、内容异 340);证据链自洽:149,255+340=149,595=restore_ids_supplied;过全部现行 S3 过滤后可回收 **142,346 曲 ≈ +183,773 段(估算,按已实现段密度外推)**。方法规划端审读通过(只读、金字塔判定、固定种子样本),restore 全程 opt-in 默认关。③lenient_measures 接线追认(初始提交的休眠能力,D45 类;现池不受影响——s2 标签先于该提交,全部严格模式产物),规划端补判决测试 3 项(tests_lenient_labels);"长度"子串闸门脆弱性记为已知瑕疵(测试夹具自己踩中过)。④三个新闸:**磁盘闸**(需 120+GB,现 91.38,restore 渲染禁启)/ **VN-unknown 闸**(composer 长尾映射到"unknown"是未验证主张,冒烟先行)/ **评测集冻结闸**(preflight 显示 restore 会 val+7,090/test+6,924 —— 不允许;若批准必须 train-only 进池,评测协议版本不动)。⑤O6 到期 + 新增 O8(restore 波次编排)交用户 | 2026-07-23 | S5_S2_RENDER.md+ROUND2_POOL_5_S2.txt+PDMX_DEDUP_AUDIT.md+RESTORE_PREFLIGHT.md+restore.json;规划端复算=对委员会文件逐项加法/含量核对 | tests_lenient_labels.py;EXECUTOR 追加 14;O6/O8 摆板 |

| D61 | **O6/O8 双裁决落地 + 二轮试验开训口令下发**。O6 = 用户方案(B-试验先行):68k 续训,硬顶 +10,000 步;规划端评估采纳,依据:①lr 非死尾(cosine 参数 warmup1500/max100k/min_ratio0.1 → 68k 处 0.315×峰、78k 处 0.206×峰,计算自 train.py 委员会代码);②主判据族(Δpitch≥+0.03 连续 3 评)在同一 lr 带由 C2 预注册过,灵敏度已知;③成本不对称(试验 ~1 天 vs 全新 ~7-9 天)。预注册钉死:变量唯一=池 v1→v2;C1a 关;lr 原调度;判决点=起点+10k 停进程;成功=主判或次判(AMT 真pitch≥0.25 且末 5 评不降)命中;**灰=败,不展期**。O8 = A 训中进池:现池开训不等 restore,restore 三闸(磁盘/VN-unknown/train-only)过后渲,QC 过随例行重启并入。追加 15 下发(起点步以恢复横幅为准、config echo 为核验件、到点硬停判决归规划端) | 2026-07-23 | 用户答复原文("先看看68k继续的效果,最多再10k,如果不好重头开始"+ O8 选 A);lr 数字=对 train.py 现行参数的算术 | ROUND2_DATA O6/O8 裁决节;EXECUTOR 追加 15;O6/O8 关案 |

| D62 | **试验首评 parseable=0,用户停训 —— 判决悬置(VOID 非 FAIL),证据令下发;restore 渲染冻结**。①统计事实:一轮 68k 的 parseable 稳定 0.08-0.12(48 样本评),真续训(同权重+同评测集)首评恰为 0 的概率按二项算 <2% —— **"数据没用"不是首要假设**。两大主嫌:(a)**恢复未发生**(全新权重首评 parseable=0 是正常现象,一轮自己前 3 万步就是这样)——判别件=启动横幅步数+训练行 loss 量级(≈60 级=全新,2-3 级=续训);(b)**评测链故障**(异常→兜底)——判别件=autolog 首评行 empty= 字段(=1.0 即链故障,D24 仪器)。试验判决在证据到达前悬置,任何人不得按"数据假设已败"行事;用户停训动作无害(两嫌之下继续跑都无意义),记录在案。②执行端受用户口头指派建了新渲染流水线,**未 push,规划端未见未审**;为何弃用既有管线(s4_parallel/s5_vn_render 带内存准入/截断守卫/断点续跑/同机纪律)无说明 —— 审计前禁跑。③restore 渲染冻结重申:磁盘闸未过(执行端自己的预检 91.38GB < 需 120GB,DISK_INVENTORY.txt 仍欠),盘满会连累全机。④本轮到货验收:VN-unknown 冒烟通过(退出码 0+产物字节数,主张兑现)、train-only 过滤交付(128,585 曲,val/test=0) | 2026-07-23 | 用户口述停训+parseable=0;概率=对一轮委员会基线的二项算术;VN_UNKNOWN_SMOKE.txt+restore_train.json | EXECUTOR 追加 16(五项证据令);O6 试验状态=悬置 |

| D63 | **本地运维全权下放 + 问话经济学(用户令)+ 磁盘闸按用户口径关闭**。①D57 三区制扩展为**本地运维自治**:内存管理/性能/路径/存储/磁盘/并发一律执行端全权,不报备不预批;规划端审计域收窄到【数据正确性与可比性】(标签-音频对齐、staging/武装纪律、train-only/评测冻结、判据与账目)。②执行端 token 余量有限,规划端问话必须短而高密:只要判别所需的最少行数,不要叙述、不要成段日志;追加 16 的五项证据令即为反例(过度索证),已作废,由追加 17 的"两行+一次 push"取代。③磁盘:用户报 130GB 余量(> 120GB 门槛),restore 磁盘闸判过;规划端上一轮按 91.38GB 旧数发的冻结警报过时,教训 = 本地易变状态(磁盘/内存)不该进规划端的闸,谁在机器上谁说了算 —— 与①同源。渲染唯一剩余前置 = 流水线正确性审计(代码 push 后规划端自读,不再要说明文档) | 2026-07-23 | 用户原话"本地的这些…交给能直接访问本地的来""问问题记得简短然后获得最大信息""130GB 的余量" | EXECUTOR 追加 17;D62 证据令瘦身;规划端行为准则更新 |

| D64 | **去重过滤考古(用户问"我是否说过可用重复被无视?还是前任的 work?")**。判定:**前任时代的无记录偏离,按本账本宪法属 bug 类,现被实锤**。①出身:subset:deduplicated 上游列过滤写在 Step 0a 脚本初版(402c299,2026-07-08,前任规划期、执行端代笔),与 all_valid/license 捆绑作"快速前滤";**SPEC R-S3.1~3.6 从未规定它**(规范只有 work_key 超额截取 R-S3.4 与 MinHash 防泄漏 R-S3.6),D1-D11 无记录 —— 按账本头规"没记录的偏离视为 bug"。②同日(07-08)前任的抢救数据战役修了别的过滤(作曲家元数据 +31,353、标题硬滤、reduced vocab),独漏此条,此后 14 天无人质询;07-22 执行端首次揭出 151,439,07-23 审计证明 98.56% 假去重。③全库检索(HANDOFF/DIAGNOSIS/CORRECTIONS/FEEDBACK/KICKOFF/CORPUS_REGEN/HAZARD/DECISIONS):**无任何"可以使用重复"的用户裁决记录**;在案的是用户一贯数据最大化立场(D12"能多点数据多点"、D18 全量找回)——该过滤方向性违背用户政策,但从未被摆上桌。④继任规划端责任面:接手后未重审 S3 前置过滤,"继承未质询"盲区与 D45"设计未接线"成对,记为审计方法论教训(接手审计清单今后必含:上游数据落地前的每一道过滤逐条对 SPEC/账本)。⑤澄清口径:151,439 是被排除的谱/MIDI 记录数,**音频尚不存在**,渲染波次才产音频;链路 151,439 → 审计非重 149,595 → 过现行 S3 过滤 142,346 → train-only 后实进池 **128,585 曲** | 2026-07-23 | git log/show 402c299 + SPEC S3 节原文 + 全文档 grep(命中为零)+ PDMX_DEDUP_AUDIT.md | 接手审计清单更新;答用户问 |

| D65 | **执行端能力波动入档 + token 断供过渡治理 + 音频容器裁决**。①用户披露:D55 前后执行端能力跃升系**换用更聪明 agent**,非学习曲线——既往对执行端的能力判断(追认制/三区自治/审计质量)全部隐含标定在该 agent 上;其 token 将尽,29 号重置,届时回退为旧 agent。②过渡期治理(至 29 号或 token 恢复):**追认制暂停、自治按保守解释、一切口令写成复制粘贴级机械步骤、不委托判断**;聪明 agent 余量只花在三样上(流水线代码 push / 试验两行 / finalize 在否一字)。③容器裁决:既有 PDMX 链终产物 = **16kHz Opus**(wav 仅中间产物,装配器不认 wav);用户令 restore 波次 wav→flac,判合规(装配器认 .flac、加载器直读、混用无碍),磁盘归执行端(D63)。**未决正确性点 = finalize 预设链在否**:不在则 restore 波次为干声、与现池系统性音色差,处置=对已存 44.1k 文件后补 finalize(免重渲),等执行端一字贴回 | 2026-07-23 | 用户原话(换 agent/29 号/wav→flac)+ render/core.py 头注与 finalize 签名 | EXECUTOR 追加 18;过渡期规矩生效 |

| D66 | **finalize 确认"在" → 干声疑虑解除,restore 波次音频合规定案**(容器 flac + 预设链完整 = 与现池同分布,仅封装不同)。余欠仍两样:流水线代码 push(git push 几乎零 token,优先)+ 试验两行。**兜底预案(若 token 先竭)**:渲染产物照常存盘,进池由规划端挂载闸把守——届时对产物跑现成审计(audit_render_qc 时长对账 + dry-run 三向账),复制粘贴级命令,旧 agent 可执行;代码审计推迟到 29 号后不阻塞渲染,只阻塞进池 | 2026-07-23 | 执行端一字贴回"在"(用户转达) | 挂载闸为唯一剩余关口 |

| D67 | **r3 流水线审计:通过(就是旧管线改造,非另起炉灶)+ 两道守卫补上 + 一处口径更正**。①审计结论:所谓"新流水线"= s5_vn_render 消费模式(--native-vn-root:官方 CLI 目录批产 MIDI/CSV,s5 只消费)+ 3 个只读预检门(MuseScore 归一化/含拍号门/ScoreData 解析门,均可续跑、拒收带审计单)。**对齐机器同一套**(tmap 出自实产 CSV、渲染同 render_midi+finalize、切片同 _slice_audio)→ 可比性保住。native 转向证据充分:首跑 vn_ok=705/vn_fail=114,502(根因=restore 原始 MXL 未归一化,主池当年过了 xml_norm),100 曲旧失败金丝雀经官方 CLI 100/100。失败隔离清单(append-only JSONL+续跑跳过)是仪表升级。②**高危洞已堵**(规划端补丁+测试 2/2):s3_filter restore 流默认写主 manifest('w' 直开无备份)→ 覆写即现役池全体丢 split/work_key 注入;现 restore 流禁写主名。s5 消费模式原可直写 pdmx_perf_labels.jsonl(绕挂载闸静默进池)→ 现强制 r3 staging 命名+显式 r3 清单。**主 manifest 是否已被覆写待执行端一条命令体检**。③composer:native 批统一 -c Bach(上游 README 建议重表征作曲家;与主线 Beethoven 缺省不一致,均为任意先验,记录接受;先前 unknown 冒烟结论被其自弃)。④**口径更正(规划端错误)**:D65 答用户"上次是 16kHz Opus"对 S4 线成立、对 s5 段音频错误 —— 07-10 起 s5 段=16kHz WAV(soundfile 写不了 opus,@109db50),行内 audio_path 使装配不受影响;用户的 wav→flac 指令对象正是它,完全正确。⑤FLAC 落盘合规(无损、装配器直读、省盘) | 2026-07-24 | VN_NATIVE_BATCH_DECISION.md + 三提交 diff + @109db50 考古;守卫=tests_r3_guards 2/2 + 全库回归绿 | r3 双守卫;EXECUTOR 追加 19(manifest 体检一条命令) |

| D68 | **主 manifest 体检通过 + r3 全链命令下发**。①体检(执行端贴回,用户转达):38,252 行 / 最后写入 7-12 —— 早于 restore 工程(7-22 起)全程,**未被覆写**;38,252 与 render_qc"查=38252"、S5_S2"入选 22501/38252"两份委员会报告分毫一致 = 盘上真基准。②规划端小勘误:追加 19 预告的"完好≈38,371"引了预检重算口径(现行过滤当下重跑的值),非盘上历史值,差 119 为过滤时点差 —— 判读以两报告交叉的 38,252 为准,预告口径记错。③追加 20 下发 r3 全链:生成 r3 清单(守卫强制独立文件名)→ 归一化/预检/官方批(执行端自治)→ s5 消费(守卫自动 r3 staging)→ 贴回 DONE+行数;**挂载(改名+dry-run)仍为规划端最后一道闸** | 2026-07-24 | 执行端两行贴回 + render_qc/S5_S2 交叉;guard=tests_r3_guards | EXECUTOR 追加 20;等消费产出后发挂载令 |

| D69 | **r3 交接验收 + 收尾工具三件套交付 + 人手执行模式**。①交接文档(HANDOFF_2026-07-25 + 失败重算)验收通过:127,741 曲中 125,633 已消费(98.3%,staging 136,946 行)、544 终败(0.4%)、~2,200 待补(1,588 有产物 + 624 补回仍被旧清单隔离);卡死根因(拍号 44 异常谱,15/57 叶 9 小时)已加双层超时;finalize 链确认未绕过;"产物目录现存数不作进度"等陷阱口径全对。②重算脚本只报告不改写 → 交付 r3_failures_release.py(存在性双路释放:有标签/有产物,终败保留,.bak 幂等,**自动发现本地文件名并打印下一步消费命令**——人手模式的关键);交付 wav2flac_labels.py(三相:逐件转换删源[盘瞬时只多一件]/标签整文件原子改写/全量存在性+可读核验;断点幂等);build_dataset 补 pdmx_perf_labels_r3_native.jsonl 武装名挂载线(实际命名以执行端 _native 版为准,取代追加 20 的猜测名)。tests_r3_finish 2/2,全库回归绿。③执行端 token 竭,**用户接手人手执行**(追加 21 五步,每条整行复制);挂载(改名+dry-run)仍为规划端最后一道闸 | 2026-07-25 | HANDOFF_2026-07-25_NATIVE_VN.md + NATIVE_VN_FAILURE_RECONCILE + 两工具沙盒测试 | 追加 21;等三样贴回后发挂载令 |

| D70 | **追加 21 验收通过 + 挂载令下发(池 v3 预登记 753,304)**。①复核:补消费三段 23+550+940=1,513 曲,staging +1,648 行(密度 1.089/曲,与波次基线吻合);失败 582+16=598 内洽(0.47%);全量核验 138,594 行全 flac、零缺失零不可读;手工 WAV→FLAC(int16 直通+逐样本 array_equal 49,711/49,711)校验口径优于规划端工具,追认;工具幂等复跑 0/0 佐证。交接盲区 15 曲正式记败,闭合。②残差 ±3(127,744 vs 旧清单 127,741)与 skipped ±2:归因 ms4 归一化清单与旧清单口径差,不作悬案 —— 挂载 dry-run(utt_id 查重+音频实检)为终审。③挂载令=改名+dry-run 两条,期望值先于数据预登记:TOTAL 753,304、train 704,549、**评测四池一字不变**(25,254/8,932/13,330/1,239)、dup/no_audio 增量 0、filtered=3。对上=池 v3 冻结;训练另等口令(试验判决仍悬置) | 2026-07-25 | 执行端五步贴回(用户转达)+ 对 D69/POOL_5 基线的算术复核 | EXECUTOR 追加 22;等 dry-run 统计块 |

| D71 | **池 v3 = 753,304 冻结,数据建设期完工 + 装配时长缓存交付 + TAST 构成裁定正常**。①终对账(用户人手 dry-run,转贴落档 ROUND2_POOL_7_R3):预登记全命中 —— TOTAL 753,304、train 704,549、评测四池一字不变、dup/no_audio 零增量、lite 差 22 容差内、撞名系既有 14,239 类。规模 = 论文 ~40-45%(立项时 1/8 → ×3.4);TAST 59,911→198,505(r3 全行带戳,S5/VN 线即 TAST 生产线)。②TAST 裁定:占论文 TAST 总量仍仅 ~27%(PDMX 侧 37%),不为过量;训练构成由混比 D2(0.20)管辖非池占比,池深化的实效 = 最薄方言过采样 2.42×→≈1×(健康);风格先验注记:r3 戳出自统一 Bach 条件的 VN 演奏(D67)。③用户批评"O(N) 探测竞赛 TLE"成立 → 持久化时长缓存交付:(size,mtime_ns) 失效键、追加式 jsonl(中断安全)、IO 全故障静默降级(缓存永不成为装配故障源);首次装配照旧建库,此后重复装配探测→ stat,预期半小时级→分钟级;tests_dur_cache 3 项(命中免探/失效重探/坏行与不可写容忍),全库回归绿 | 2026-07-25 | 用户转贴 dry-run 原文 + D70 预登记逐项比对 | ROUND2_POOL_7_R3.txt;build_dataset 持久缓存;下一议程=试验尸检两行→开训方案 |

| D72 | **试验尸检结案:恢复成功、判决 VOID(仪表故障非数据失败)+ 评测仪表三补 + 试验重启令(池 v3)**。①两行日志定案:step 68050、lrE=3.14e-05=峰×0.314 与规划端预告 0.315 分毫吻合、四方言 sem 2.2-2.5 一轮末水平 → **恢复真实发生**,"没加载权重"假设死亡;首评 n=2(1200s 预算 ÷ ~600s/样本),真率 0.10 下 2 条全空概率 81% → parseable=0.00 无统计力,试验 VOID。同权重 07-22 n=48 时 0.10 为铁证:非二轮新问题、非数据失败(探针 acc=0.69 健康)。②执行端六项观察逐条裁定:#1"停在时间戳一半"系 **raw[:160] 日志显示截断**(infer.py:309,时间戳是单 token 不可能写半个)——目击证据作废;#2 兜底串判读正确;#3 四拒因同源推断降级为待检假设(靠新仪表判);#4 TAST 长度撞 max_new=900(中位 882/超限 48.3%)是独立实测,**立为新案**(或部分解释一轮 parseable 平台与 DYCK 头号地位;注意评测走 20s 窗协议,全曲口径≠单窗口径,且 1023 位置表先于 900 生效);#5 n=2 无意义判读正确;#6 ts=0.0000 待与一轮日志同位对比(挂账)。600s/样本两大嫌:慢路径(代码有"退全量 forward 慢十倍"分支,触发必打 ⚠ 行,一条 findstr 定案)/ 渲染并发抢 GPU(时间线吻合)。③仪表三补(全库回归绿):LAST_GEN_STATS(n_new/停因 eot|cap|poslimit/快慢路径)、拒绝现场随行 gen 统计 + raw 显示扩到 320 并注明"显示截断非生成截断"、eval 汇总 n<12 印 NA(勿判读)。④tokenizer 四文件之谜 → 哈希比对令(训练默认加载 work/rubato_spm.model);⑤试验重启令:D61 方案原样,变量=池 v1→v3,判据不动。执行端报告文化(自证伪两条如实列出)记为样板 | 2026-07-25 | 执行端报告(用户转贴)+ infer.py:309/186/246 代码事实 + abtest/train_full 时间线 | infer.py 仪表 + train.py NA 守卫;EXECUTOR 追加 23;ts=0 对比挂账 |

| D73 | **TAST 长度案撤案 + 正典 tokenizer 钉板 = work/rubato_spm.model**。①执行端四哈希两两不同,全库 4 处默认加载点(build_dataset:292/eval_final:42/s6:94/sop_next:151)一致指向 rubato_spm.model;用正主重测同 300 行:TAST 中位 389/p90 647/超 900 仅 2.0%(A2S 中位 154)—— **max_new=900 对 TAST 中位有 2.3× 余量,长度不构成瓶颈**,前表系误用 v2 所测,#4 撤案(执行端第四条自证伪推断;纠错循环运转良好)。"截断解释一轮 parseable 平台/DYCK 头号"假设随之失去主证,留给 LAST_GEN_STATS 仪表下次评测直接判停因。②一致性证明:train.py:201-203 的 token id 越界断言(历史词表不一致事故的产物)在一轮 68k 步全程零触发 = 数据编码端与模型嵌入表同源自证;eval raw 中时间戳以单 token 出现佐证。③v2/new/tokenizer.model 三文件裁定为词表重建期(D9 前后)的陈旧候选,未被任何在用代码加载;v2 对同文本 2.3× 膨胀不再深究成因(与在用链路无关),但其存在已造成一次误测 —— 建议(不令,属本地运维)归档挪走三个陈旧文件防再踩。④仍欠:findstr 慢路径结果、试验重启回执(追加 23 之 2/4) | 2026-07-25 | 执行端续报四哈希+双口径重测表(用户转贴);断言零触发=68k 步委员会日志的反证 | 长度案关;600s 之谜与重启待回执 |

| D74 | **600s 之谜结案(渲染抢 GPU)+ O6 决策口径向用户重申(无变更)**。①findstr 零命中 = 慢路径未触发,快路径在用;旁证时间线:train_r2_trial 末写 07-23 17:51 与 s5_r3_restore 末写 18:00 重叠 —— 600s/样本 = 试验评测与 restore 渲染并发抢 GPU 所致,结案;本次重启已明令训练期禁渲染。②用户问"二轮不是说好从头开始吗":澄清 —— "从头开始"是规划端在 O6 的【推荐项 A】,从未成为决议;**现行决议 = 用户自己的方案**(D61 原话"先看看68k继续的效果,最多再10k,如果不好重头开始"),规划端评估后采纳。10k 续跑的性质 = **决定二轮起点的判决实验**,不是正式二轮承诺:判据命中→就地转正为二轮(继续跑);未命中→按预注册转全新热启(池 v3 照用)。它能回答的问题是"一轮权重的病灶是否缺数据"(冻结指标是否被 2.3× 池唤醒),不能回答"全新二轮的天花板"——负结果不证明数据无用,故失败分支是"带新池从头练"而非"放弃数据"。三旧 spm 文件用户将归档(D73 建议采纳) | 2026-07-25 | findstr 退出码 1 + 双日志末写时间戳(用户转贴);D61 用户原话引用 | 等用户确认后跑追加 23 第 4 条 |

| D75 | **试验配置漂移叫停(clip 1.0/lr_dec 5e-4 ≠ 一轮工作点 25/3e-4)+ 正典启动命令入库 + 70,000 评测转正为基线**。①抓法:开局回执 config echo 对照一轮存档 RESTART_C2.txt 回显(clip_norm=25.0 lr_dec=3.0e-04)—— 两次试验启动均漏两旗标(68,050 次的 lrD=1.57e-04 同样出卖 5e-4 基数)。clip 1.0 之害:本窗实测 gn 13-45,每步被压至范数 1.0,更新幅度 = 工作点的 1/13~1/45,近冻结 → 跑满 10k 必假失败,数据假设无从显形。②根因:例行启动命令只存在于旧执行端记忆,从未入库 —— 频道规矩的盲区(早于规矩成文的口头惯例);处置 = 正典命令钉进 EXECUTOR/ROUND2_DATA,今后一切重启唯此一条。③计时重置:漂移下 ~1,800 步作废(权重近冻结,留在模型里无害);判决点 = 新恢复步 + 10,000。④副产品转正:70,000 评测 = 试验基线(权重≈68k 态):评测机 48/48 复活(~13 分/集,GPU 抢占理论实锤);LAST_GEN_STATS 首秀即破案 —— 拒样本 n_new=642/stop=eot/fast=True,自然收尾仍 DYCK+MEASURE 败 = **内容伤非长度伤**(截断假设在真数据上出局);maestro 真pitch 0.17 = 冻结区起点;omr_ned 0.787(n=2,不判读);TAST 滚动 sem ~2.0 系 r3 民谣成分效应;混比全线欠采(TAST 0.77× vs 一轮过采 2.42×)= 池深化目标兑现;超长过滤弃 33,722 对有账 | 2026-07-25 | 用户转贴开局日志 vs RESTART_C2.txt 委员会存档 | EXECUTOR 追加 24;正典命令双入库;等重启回执 |

| D76 | **"说人话"规矩成文 + 用户启动块收编为正典**。用户批评成立:规划端长期发"例行命令原样跑"式谜语 —— 工作目录/解释器/日志去向等隐含上下文只存在于旧执行端记忆,人无法执行,这正是"需要一个 executor 来解释规划端模糊指令"的根源(与 D75 命令未入库同病)。标准成文:**规划端下发的每条命令必须零上下文可粘贴执行**(完整路径、显式工作目录、日志去向、停法),做不到视为规划端失职。用户自写的 Start-Process 启动块逐项核对正确(nemo_test 解释器/双旗标/repo 工作目录/-u/日志分流/PID 留存),且优于规划端原一行版,原样收编为正典命令(EXECUTOR 追加 24 更新)。停法升级:优先 Stop-Process -Id <PID>,扫杀为兜底 | 2026-07-25 | 用户原话与其启动块;逐项对照 D75 工作点 | 正典命令 v2(完整块);规划端指令风格永久更正 |

| D77 | **v3b 启动验收通过(正典配置生效,判决点 80,800)+ 双节奏评测交付 + 两条即时判读**。①验收:clip_norm=25.0/lr_dec=3.0e-04 ✓,恢复 70,800、lr 重刷值与调度公式吻合;时长缓存首战 37.3 万条秒装(D71 交付兑现)。②71,000 评(正确配置仅 200 步)= 基线复证:parseable 0.08(n=48,回一轮带)、maestro 真pitch 0.18 冻结区、评测机健康(~18s/样本)。仪表新读数:本次拒样本 n_new=900/stop=**cap**/fast=True,内容为 1/16 音型交替复读循环 —— **撞上限是复读病的症状不是长内容**(与 70,000 的 eot-stop 样本并列:两类停因并存,复读循环是 parseable 的真敌人,与 D46 DYCK 头号一致)。③用户问"评测太慢":交付双节奏 —— 探针(教师强制,秒级,试验主判据唯一依赖)保持每 1000 步;解码腿(48×生成,~25 分钟)按 --eval-decode-every(缺省 0=同步;试验用 5000)稀疏跑;probe_only 评不进止损器/不评 best.pt/照常落盘 autolog;试验开销 ~5h→~1.5h;判据与判决点不变(评测节奏=仪表非训练变量,换装重启不重置计时)。④"继续训练还有意义吗"答复入档:有 —— 正确配置下的试验刚从 70,800 起算,此前步数或近冻结(clip 1.0)或被渲染污染(GPU 抢占),预注册问题(2.3× 池能否解冻 maestro pitch)尚无任何数据回答;弃跑=不买证据直接付全新热启的全价 | 2026-07-25 | 用户转贴 v3b 开局日志 + 委员会调度公式复算 | train.py decode_legs/probe_only + build_dataset --eval-decode-every(回归全绿);EXECUTOR 追加 25(换装可选) |

| D78 | **O6 试验判决:失败(提前于 80,800 判定,判据数学死亡非展期)→ 二轮正式全新热启(池 v3)**。①判据可达性核算(8 次探针,72k-79k):maestro Δpitch 序列 −0.05/−0.04/+0.04/+0.03/−0.01/+0.00/−0.05/+0.00 —— 最好两连正(74-75k)后回落,余下仅 1 评,三连正不可达;副判 AMT 真pitch 全程 0.16-0.20(需 ≥0.25 且末五评不降)同不可达 → 按预注册判败,早停=止损非目标漂移(用户提议,规划端核算后批准)。②试验遗产:合成侧被大池喂活(A2S 滚动 2.41→2.32、TAST 1.94 新低、合成探针真pitch 至 0.82);maestro Δsem +0.01→+0.08 微弱正效应但音高冻结 —— **结论:一轮权重对真实音频的聋是权重级顽疾,尾部 lr + 数据不可治;数据需在权重成形前在场 = 全新热启的存在理由**。③二轮配方:一轮配方 + 两处记录在案的差异 —— 池 v1→v3(主变量)、clip 25/lr_dec 3e-4 自第 0 步(一轮 H1/H2 已判定的终点值,不重走弯路);C1a 仍关(训中独立变量);max_steps 100k(753k 池 ≈ 7.2 epoch,论文式少 epoch 多数据);评测双节奏(探针 1000/解码腿 5000)。④机械保障:ckpt 目录改名归档 ckpt_r2trial_79k(试验权重保留),空目录起跑;开局核对=无"续训:恢复"行 + 三参数回显 | 2026-07-26 | 用户上传 v3c 全量日志(281 行,8 评探针序列规划端逐行提取核算) | EXECUTOR 追加 26;二轮正式开跑;O6 关案 |

| D79 | **"不听音频"作战梯子立卡(EXPERIMENT_LISTEN)**。用户问"没法解决只能祈祷?"——答:病理已定位(听觉通路机械完好[合成侧 Δpitch +0.3~0.65],病灶=抄近路竞争:文本先验 + 高重复池背题都比听便宜),对策成梯:0 级大池从零(跑着)→ 1a C1a 增广(已建待启)→ 1b 音高 token 加权 loss(129 MIDI ID 可识别,便宜,20k 冻则交付)→ 1c 教师强制输入 dropout → 2a 编码器音高辅助头(帧级多标签,MIDI 对齐标签现成,模态塌缩经典解法)→ 2b AMT 课程表(动 D2 需用户批)→ 3 架构级(不建)。仪表现成:maestro Δpitch 主表 + stop=cap 占比晴雨表。规矩:逐级单变量、判据预注册、每级是实验不是信仰 | 2026-07-26 | 病理引 D28/D29/D44 仪表与本周试验探针序列 | EXPERIMENT_LISTEN.md;二轮报告点按卡读表取牌 |

| D80 | **用户令:C1a 增广进二轮基线(自第 0 步),分段进场方案作废**。用户理由被采纳并记档:试验 9,000 步(池 v3+无增广+尾部 lr)已构成无增广侧的实测,该层归因已买单;C1a 本属论文配方(其每曲 16 音色变体的穷人版),非待验证猜想。二轮配置终版:池 v3 + 全新热启 + clip 25 + lr_dec 3e-4 + 双节奏评测 + **C1a 开**;C1a 生效自证 = 回显 aug_acoustic=开(代码写,非人证)。梯子卡相应更新:1a 并入 0 级基线;下一张牌 = 1b 音高加权(规划端 10k 前建好备用,20k 冻则进) | 2026-07-26 | 用户原话;tests_c1a 4/4 复跑绿;旗标接线 build_dataset:330→646→train:829 三点核对 | EXECUTOR 追加 27 终版命令;EXPERIMENT_LISTEN 0 级含 1a |

| D81 | **梯子改配方制(用户令)+ 三件套建造承诺**。用户论点采纳:音高加权/遮上文/辅助头均系文献常规手段,不再逐级排队 —— 温和剂量打包进场。执行排法(总时间最短,GPU 零空转):①v4 今日启动(已含数据+从零+C1a 三大牌);②规划端并行建三件套 —— C1a 加强版(谱形/动态类"换琴"方向扰动)、音高 token 加权(×2~3 温和剂量)、教师强制上文 dropout(低比率+爬坡,文献标准防早期崩)——全部带测试,**10k 检查点前交付**(D48 承诺纪律),随例行重启打包进场;③辅助头(2a)单独把关(唯一加新部件+新损失平衡的重项,权重配错会早期抢梯度),并行开建,30k 冻则进。④护栏预注册:打包进场后合成侧 A2S 轨迹明显落后一轮已知基线 → 停、二分排查(归因成本从预付改或有负债,知情下注)。EXPERIMENT_LISTEN 卡随改 | 2026-07-26 | 用户原话("llm论文里很常用");一轮速度双败(D40/D41)为打包风险的历史注脚 | 三件套 = 规划端当前最高优先建造项;v4 启动不等 |

| D83 | **C1a 加强版砍案(用户令:不明风险不进长跑)+ 二轮终配方钉板 + 即刻启动**。二轮配方终版:池 v3 + 全新热启 + C1a 现版(D80)+ 音高加权 ×2.5(D82,自带实效爬坡:均匀 CE 下归一化加权≈无操作)+ clip 25 + lr_dec 3e-4 + 双节奏评测;遮上文 dropout 续建,10k 检查点交用户裁决进否;加强版 C1a 停止建造。启动核对五项:clip_norm=25.0 / lr_dec=3.0e-04 / eval_decode_every=5000 / aug_acoustic=开 / "音高 piece 掩码…加权 ×2.5"行,且无恢复行 | 2026-07-26 | 用户原话("太虚""先开始训练吧") | EXECUTOR 追加 28 终版命令;开跑 |

| D85 | **二轮 60k 判读(答用户"parseable=0=参数问题?")+ 执行端 16 提交概要验收(逐项审后补)**。①judgment:parseable=0(60k,真 0,n=48)**不是**训练参数问题 —— 执行端 61200 A/B(同 ckpt/同样本/同验证器,greedy vs beam4,含域 prompt 臂)排除解码配置/上限/验证器误杀(四条标准答案同验证器同预算全过);真凶=自由生成不闭合(暴露偏差):教师强制侧在进步(pv 2.81→2.54 [36k-61k 502 点分箱]、**maestro 真pitch 0.28-0.34 = 一轮冻结区 0.15-0.20 解冻**、amt_f1 项目史首次非零 2.6-5.5%、拒样本 raw 已是结构化变化音乐非复读环),自由生成循环/漏戳/不收尾。跨代注意:TS_* 验证类 07-28 新增,一轮 0.08-0.12 系旧松验证器口径,直比不成立。②辅助头 A/B(35400→35500,100 步门,原子快照双臂):头学习通过(af1 +45%,aux BCE −33%,开销 +5%),解码器传导未见(Δsem B−A 一致微负)→ 按预注册走"tune once"分支:下一杆=解码器/交叉注意力音频依赖损失,不并蒸馏、不吹牛——**小步快门范式立为样板**。③执行端另修:Beyer 黑名单历史空转(旧代码按"beyer"子串搜路径永远返回空)→ 换 Beyer-Dai 公开 14 作品名单 + 65 曲内容隔离(configs/pdmx_content_quarantine)+ 精确泄漏认证器;D84 累积遥测/精确续训修复;WDDM 缓存修复;解码器长度边界守卫;nASAP test 工作键改读 nasap_split.json(旧 1058 系演奏误计)。**16 提交逐项验收 + 相应池影响复核挂下轮**(本轮先概要追认:方向与证据纪律全部合格)。④下一步排序:a 解码侧扫参(rep-penalty/EOT 校准,固定 ckpt 零训练风险,执行端已点名)b 遮上文/scheduled sampling(1c 由可选转点名:治暴露偏差对症)c 交叉注意力音频依赖损失 d 训练 60k→100k 不停 | 2026-08-03 | eval_autolog 55k-61k + DECODE_AB_STEP61200.md + EXPERIMENT_AMT_DISTILL.md(全部委员会文件) | 答用户;16 提交细审 + 1c 建造挂下轮 |

| D86 | **"当前端能做的全部做掉"批交付(用户令)+ 16 提交细审追认(D85 挂账兑现)**。①细审追认:执行端 16 提交(07-27→08-03,~9,117 行/80 文件)沙盒全量回归 **75 测试文件全绿**(唯一环境红项 tests_leakage_certificate 系沙盒缺 partitura,pip 装上即 6/6,非代码缺陷);TRAINING_CODE_REVIEW 12 缺陷修复逐项对码核过(累积改 micro_mean×序列数、批游标精确续训、坏快照 fail-closed、泄漏过滤 fail-open→closed、Beyer 14 作品名单+65 曲隔离、train.yaml 真接线、音高掩码尺寸 fail-closed 加固我 D82 代码并补测试)——**全部追认,零回滚项**。②扫参工具交付(D85④a):infer.py 把 eot_boost 穿进 greedy/beam 两臂(rep_penalty 原有,一并穿透 single_window_tast/infer_a2s);build_dataset --decode-abtest-rep/-eot 逗号表 → (beam×rep×eot) 笛卡尔网格逐臂跑同 ckpt 同子集,JSON+autolog 每臂记 rep/eot;缺省网格=旧两臂解码行为与产物文件名不变(JSON 仅增记录字段),扫参网格出 DECODE_SWEEP_STEP{n}.json。③1c 遮上文交付(D85④b):--input-dropout/--input-dropout-ramp(按已完成步线性爬坡);可遮位=内容 token(loss_mask 右移一位),prompt+首位免疫;替换 unk 经 resolve_unk_id fail-closed;标签/监督面零改动;日志 id= 列=实际命中率;tests_input_dropout 6 绿。④2c 音频依赖损失交付(D85④c):--audio-dep-weight/-margin;批内 roll encoder 状态做第二次 decoder-only forward(transf_decoder+log_softmax,infer 快速路径同成员,缺则 RuntimeError 不静默),sem 掩码逐序列 CE,relu(margin+匹配−错配).mean()×权(与 acoustic_aux.alignment_loss 同约定,主 CE 锚住匹配侧);B<2 或可计分<2 跳过计数;日志 ad= 列=gap(decoder 听没听直读);tests_audio_dep 6 绿。⑤入场纪律:两杆缺省 0=位级惰性(有测试钉死),不自动进正跑,各经 100 步 A/B 门(照 EXPERIMENT_AMT_DISTILL 原子快照双臂模板)再启用;扫参零训练风险,自然重启窗即跑。⑥梯子刷新(1b 已生效/2a 已判转 2c/中期更新节)+ EXECUTOR 追加 29 | 2026-08-03 | 本轮 diff;tests_input_dropout.py/tests_audio_dep.py;回归 83/83(沙盒);TRAINING_CODE_REVIEW_2026-07-27.md | infer.py+build_dataset.py 扫参;train.py 1c+2c;两新测试文件;EXPERIMENT_LISTEN 刷新;EXECUTOR 追加 29 |

| D87 | **暂停窗验收(扫参 16 臂 + 双杆安全门)= 追认执行端判定与回滚;2c 降级纯仪表;1c 半剂量重门提案**。①验收:执行端全程按追加 29 预登记执行 —— 同一 last.pt(63800,batch_cursor 174414)分叉双臂、GPU 串行、门未过即不晋级、主线从未动的 63800 快照素跑恢复(恢复证书行在其报告引用);三份 err 日志 Traceback/NaN/CUDA error 全零;判定数字与原始日志逐项吻合(A tc 7.7s/avg50 43.3288,B 13.6s/52.3447,+76.6%/+20.8% vs 限 +20%/+10%)→ **判定追认,教科书式执行**。②扫参判读:13/16 臂 parseable=0/24;eot=4 三臂各 1/24 且同一条 nasap_LeeN01M(仅一窗过);DYCK 23-24/24 全网格不动 → **解码侧调参不治本,关案**,曝光偏差结论加固;附注 eot=4 令 TS_PARSE/parse_error 6→3、收口提前(臂耗时 250s→210s);但 JSON 取证显示救回样本 n_new=49/stop=eot/NED=0.83 = **截肢式技术性通过非质量通过**,eot 加成不作推理配方推荐,仅存档。③关键分解(执行端报告未展开,规划端从提交日志算出):**ad=+0.829→+0.862 = 项目首次直接测得 decoder 音频依赖度** —— 训练混合(~93% 合成)上错配音频令内容 CE +0.83 nat ⇒ decoder 在合成域不聋(与合成探针 Δpitch +0.3~0.65 一致),聋是真钢琴域特异(maestro Δpitch≈0);由此 margin 0.1 ≪ gap ⇒ **2c 罚项全程零值空转**,B 臂 +76.6% 步时只买了仪表读数;loss +20.8% ≈ 全部来自 1c,构成 sem +0.39 / ts +1.01 / pv +0.49(ts 是 sem 的 2.6 倍:时间戳最靠抄上文;而 TS_MISSING 是拒因第二名,遮 ts 可能正中要害,数字大≠伤害);B 臂 100 步 avg50 55.6→52.3 快速适应 = 非失稳。D81 打包意外收益:loss 差≈纯 1c、tc 差≈纯 2c,一臂拆出两个单变量读数。④处置:a) 2c 训练版搁置(定向真音频域需按域掩码重设计,另立项);交付 **ad= 纯仪表** --audio-dep-monitor-every N(weight=0 时每 N 步 no_grad 测 gap,不进梯度,开销≈77%/N,N=50≈+1.5%;weight>0 忽略本项);b) 1c 半剂量重门(追加 30,判据自 D86 实测重标定、先于新数据登记):p=0.05,门=不炸 / id=0.05±0.01 / tc≤A×1.08 / loss≤A×1.12(实测 +20.8% 线性折半≈+10.4%,留非线性余量);过门 v6 = v4 + 1c(0.05) + ad 仪表(50),疗效由探针/解码腿判,不由门判;c) 主线素跑继续,不等门。⑤门设计自省入档:D86 loss 门把"目标按设计变难的机械通胀"误当失稳上限 —— 1c 的语义就是抬 loss;安全门该拦炸/发散/失速,剂量-疗效留给仪表。重标不是移球门:D86 的 FAIL 处置(不晋级)维持,新门是新实验的新预登记 | 2026-08-03 | reports/D86_PAUSE_WINDOW_EXPERIMENT_2026-08-04.md + 三对 out/err 原始日志 + DECODE_SWEEP_STEP63800.json(全部已提交,数字逐项复核) | train.py+build_dataset.py ad 仪表模式;tests_audio_dep +2;EXECUTOR 追加 30;梯子/METRICS 刷新 |

| D88 | **训练集自由解码诊断验收(执行端自建,追认)= parseable≈0 的"数据量/泛化"解释出局,病灶两层定位;答用户"是不是方法/理论问题"**。①工具追认:--train-free-decode 复用正式 run_eval_hooks 解码/验证/合并链,仅换抽样源(泄漏认证 train split、限有 TAST 标签行、sha256 确定性、real/synth 分组防合并遮蔽 —— 分组设计正确且关键);run_eval_hooks 补 violation_tally 入 metrics;tests_train_free_decode 沙盒过;autolog 增量未随提交,不追(JSON 足量)。②数据(TRAIN_FREE_DECODE_STEP70000.json @ 70k):nasap-real 训练样本 parseable 1/48,empty 97.9%,拒因 DYCK=43/TERMINAL=40/TS_MISSING=38;pdmx-synth 训练样本 2/48,empty 93.8%,拒因 **MEASURE=40 一家独大**(DYCK 仅 8/TERMINAL 6/TS_MISSING 5)。③判决:至 70k 每条训练样本已见 ~5 epoch,仍与 val 同水平失败 ⇒ **parseable≈0 与数据量/泛化无关,是序列生成能力问题**;缺 55-60% 数据、缺 16 音色解释不了这个 0(它们咬的是别处,见④层2)。④病灶两层(real/synth 拒因谱分裂是本诊断最大收获):〔层1·通用〕合成域上结构纪律基本在(括号/终止/时间戳大多正常),死在 MEASURE 自洽(自报拍号≠自写内容加总)= 计数类能力;误差复利算术:L≈400 token 窗、逐 token 结构错误率 1% → 全窗通过率 0.99⁴⁰⁰≈1.8% —— **现 2-4% parseable 恰是 ~99% 逐 token 纪律的数学像;parseable 是悬崖指标,不到临界不动**,与 sem 2.38-2.40 仍高于 2.0 里程碑(METRICS 参考线)互证"还没到"而非"永远到不了"。〔层2·真音频特异〕real 训练样本纪律全面崩 = 音频条件化崩溃,与 maestro 探针聋区同源 —— 16 音色/真实数据缺口咬在这层。⑤答用户假设:a)"超参"非主嫌 —— 优化器侧已对齐三家一手配方包络(REF_EXTERNAL_RECIPES);b)"方法/理论不懂"部分成立但责任在论文:Rubato 优化器/技巧只字未提;邻域 LEGATO 在 214K 样本花 67k 步才到 ABC CER 23%,符号谱生成绝对难度如此;已知未做清单=1c(排期中,对症层1自洽)+ 终评 beam(论文 beam 4,未建)+ 论文未写技巧(不可知)。⑥下一杆候选不立即建:1d 时值 token 加权(同 1b 机械)—— 先要执行端一行验证 SPM 词表中时值 `\d+/\d+` 确为单 piece(calib_score 接口猜错之鉴);且 MEASURE_SUM 是自洽约束,教师强制加权只间接受益,1c 才是正主。⑦预注册预测(先于 100k 数据,敢错):sem 斜率(2.70@40k→2.38@64k)外推,素跑 100k 终点 sem≈2.2-2.3 仍在能力带(1.5-1.9)之上 ⇒ **素跑终点 parseable 大概率仍 <10%**;若届时如此,处置=1c 进场 + 续跑预算评估(外部步数视野 67k-225k)+ 层2 靠真实域数据/音色补强(O7 届时重开),不是判死 | 2026-08-04 | TRAIN_FREE_DECODE_STEP70000.json + 2fadfd3 diff 全审 + REF_EXTERNAL_RECIPES.md + intermo/core.py MEASURE_SUM 定义 | 诊断关键结论进梯子病理;1d 挂词表验证;100k 判读预测入档 |

| D89 | **LEGATO 开源库全读(答用户"它有什么技巧")+ 优雅指标针交付**。①逐文件结论(REF_EXTERNAL_RECIPES §6,~1,400 行核心全核):**几乎零训练技巧** —— 原版 HF Seq2SeqTrainer(子类只管冻结塔存档/DeepSpeed/generate 路由)、纯逐 token CE(无 label smoothing)、零数据增广(仅高页 25% 重叠切块)、超参靠 HF 缺省(clip 1.0/linear+warmup 0.03)、无 scheduled sampling 无课程表无序列损失;真正的"招"只有评测制度:每 5000 步自由生成 mini-val 算 SER,**best ckpt 按自由生成质量选**;指标全为编辑距离类优雅降级,ABC→XML 失败样本走 fail_mask 单列 —— **它也有不可解析输出,只是测量制度不用硬门归零**;终评 beam 10 + rep 1.1(与我们已建路线一致);多页长谱训练时截断监督(max 2048)而非过滤。②三条启示入档:纯 CE+步数足以长出自由生成(101M decoder 从零,无曝光偏差处理 → 1c 定位=加速器非必需品);我们 parseable=0 与它 CER 23.3% 部分是**测量制度差**,同水平模型两种读数;它最强的牌(836M 预训练视觉塔冻结白嫖)不可复制,encoder 差距是结构性的。③交付 **val_text_ned_raw 优雅针**:infer.py 增 LAST_RAW_WINDOWS 旁路(拒绝窗保留截断后原文,infer_a2s/infer_amt 入口清零),run_eval_hooks 全样本进分母 —— 通过样本用 pred、拒绝样本用逐窗原文剥戳拼接、空输出=1.0,对参照算 NED;不冒充 OMR-NED、不参与 best.pt 选优,eval 行新增 raw_ned=/raw_scored=;自由生成的【内容】从此有连续读数,不再躲在 parseable 悬崖后(70k 训练集诊断里 synth 两条可解析样本 NED 0.38 暗示内容早在进步,悬崖挡住了)。tests_raw_ned 3 项绿(拒绝窗留原文+入口清零 / 零可解析下 raw 针出数 / 通过样本两针同值) | 2026-08-04 | github.com/guang-yng/legato@main 全库(trainer.py/train.py/configs/modeling/image_processing/inference.py/README 逐文件);REF_EXTERNAL_RECIPES §6 | infer.py+train.py raw 针;tests_raw_ned.py;REF §6;梯子/METRICS 刷新 |

| D90 | **答用户"该从零而不是热启动?根本错了?"= D1 偏离分半判定 + 论文 §2 第 43 行发现疑似缺失技巧(形式语言 pre-pretraining),立为 round-3 设计要素**。①事实钉板:论文 §3 原文 "using the **architecture** of Canary-180M-Flash…**trained from scratch**" —— 只借架构不借权重;我们的热启动 = D1(项目第 1 号决策,理由"没有 ~8.5 万 GPU 小时",SPEC 偏离清单冻结在案,后已降级为 build_model(from_scratch=) 开关)——是记录在案的资源权衡,不是失误。②分半判定:**encoder 半边安全**,有仪表实证(合成探针 Δpitch +0.3~0.65 / nasap 教师强制 pitch 0.90 / 辅助头 af1 / ad=+0.83,全部证明 encoder 在携带音高)且与全部外部先验一致(Canary 官方自身热启 encoder"helped converge";LEGATO 冻结 836M 预训练 encoder);**decoder 半边存疑**:R-S10.2 只换词表、保留 canary decoder 内部层(英语/语音翻译先验),而三家参照 decoder 全部从零,且项目全部残余病灶(收口/DYCK/MEASURE)恰都在 decoder 侧 —— 嫌疑成立、未定罪(教师强制四方言均在学,无对照实验)。③论文第 43 行发现(疑似"没写的技巧"):原文把音高状态钉为 k-Shuffle-Dyck 语言,并引 Hu et al. 2025(ACL,"Between circuits and Chomsky: pre-pretraining on formal languages imparts linguistic biases")称其为"pre-pretraining 增益最大的形式语言类"。三向汇聚:real 头号拒因=DYCK / synth 头号=MEASURE(区间求和,同为形式约束)/ 论文表示节最长段落与引文恰在这两性质上。推论:**decoder 从零 + 形式语言(Dyck+小节算术)合成语料 pre-pretraining 是自洽包**(论文暗示+Hu 实证+我们拒因谱三方支持);语料纯文本、零音频零渲染、intermo 生成器/验证器现成,可无限量自造。④处置:a) 不推倒现跑(70k 权重是仅有资产,100k 终点判读照 D88⑦);b) round-3 基线设计预登记:encoder 热启保留 + decoder 从零 + 形式语言 pre-pretraining(from_scratch 开关现成);c) 语料生成器立为下一杆候选,等用户点头即建(纯沙盒活,不碰训练) | 2026-08-04 | 论文 md 第 43/53 行原文;DECISIONS D1;SPEC.md 偏离清单+R-S10.2;REF_EXTERNAL_RECIPES §2/§6;D88 拒因谱 | 梯子加 R3 行;等用户裁决语料生成器 |

| D91 | **规划端检索失误认领(用户拍出)+ 形式语言预训练全链交付**。①失误入档:论文 §2 第 43 行的 pre-pretraining 暗示(k-Shuffle-Dyck + Hu et al. 2025 引文)**用户读论文时就专门查过该引文**,并合理默认规划端已消化;规划端两轮通读(07-14 抽配方建 REF、08-04 LEGATO 对照)均把该句当表示动机跳过,且多次断言"论文训练技巧只字未提"——**断言错误,已在 REF §5 勘误**。永久教训入规:表示/机理章节的引文 = 配方候选,每条引文必须过一遍"这能不能变成训练阶段";规划端的"仔细读过"以能列出全部可行动暗示为准,不以读完为准。②全链交付(纯沙盒,零碰主线):`gen_formal_corpus.py`(随机 ScoreIR→生产序列化器→**逐条生产验证器回验 fail-closed**;A2S/TAST 各半、变拍号调号、和弦、跨时刻长音=真嵌套;3,000 条压测 7 秒零违规;首版兜底补音踩中跨小节延音双开 bug,被自家 fail-closed 逮住修复 = 机制自证)+ `pretrain_decoder.py`(只训 transf_decoder+log_softmax、零上下文交叉注意力=decoder-only 纯 LM、目标序列经生产同一 encode_target 零漂移、AdamW 3e-4+warmup+clip 1.0、产出 decoder_init.pt)+ `build_model(decoder_init=)`/`--decoder-init`(词表换形后 strict 载入,回显自证)。测试 4+3 绿。③判据预登记(EXPERIMENT_PREPRETRAIN.md):预训练健康门 末 avg50 ≤1.5 PASS /1.5-3.0 灰/>3.0 无效;round-3 入场判读用 round-2 历史曲线作对照(10k/20k 处 DYCK 拒因占比↓ + raw_ned↓ + 探针 sem 劣化 ≤5%,两胜入配方),不烧整条 A/B。④执行节奏(追加 31):语料生成 CPU 随时可跑;预训练冒烟 CPU 可与主线并行;全量预训练(~20k 步,GPU ~2h)排 GPU 空窗,产物入库备用 —— **round-3 是否触发仍由 100k 判读决定(D88⑦),本交付只是把选项做成现货** | 2026-08-04 | 论文 md 43/53 行;用户原话(曾查 Hu et al.);tests_formal_corpus+tests_pretrain_decoder;3k 压测 log | EXPERIMENT_PREPRETRAIN.md;EXECUTOR 追加 31;REF §5 勘误;梯子 R3 行更新 |

| D92 | **20k 形式语言预训练验收:工件合格入库(round-3 备用)+ 执行端 9 项加固追认(含两处对规划端设计的实质纠正)+ CE 门标定失误认领**。①加固追认(PREPRETRAIN_INFRA_AND_SMOKE 九项全录),其中两处纠正规划端:a) 我的预训练器默认**继承 canary decoder 权重**,与 D90 预登记的"decoder 从零+语法预训练"自洽包相悖 —— 执行端改 --init-mode scratch 为缺省(canary 留作显式消融臂),正确,追认;b) 我的健康门只有教师强制 CE —— 执行端加 **decoder-only 自由续写门**(前缀→自由生成→生产验证器),直击本项目 TF/自由分裂的病根,追认并升格为主门。其余:原子写+防覆盖、精确续训(优化器/双 RNG/窗口 + 语料/tokenizer 哈希与配置严格签名,不一致拒绝近似恢复)、BF16+非有限即崩、工件角色元数据+装载端拒收 smoke/不完整工件、默认模型路径纠错(冒烟逮住我写错的 repo 内路径)、步日志增维、恢复计时口径修正。②结果判读:20k 步 47.8 分钟,零 NaN/OOM/跳行;末 avg50 CE **2.006 = 按预登记 GRAY**(≤1.5 未达)——**标定失误在规划端**:语料的具体音高是不可压缩随机熵,CE 有结构地板,≤1.5 没算这一项;本工件的目的指标在自由续写门:**0/4 → 3/4 可解析、拒因谱从 {parse+DYCK+MEASURE} 收缩到 {MEASURE×1}、EOT 4/4** —— decoder 在纯语法域学会了收口(DYCK 纪律出现在自由生成里,这正是主线缺的能力)。n=4 小样本,执行端自持保留(run-safety 门非科学结论)正确。**裁定:decoder_init.pt(SHA 6DFA62AF…,含精确续训伴生件)合格入库 round-3 备用;不得注入当前跑(执行端已自守,主线从验证过的 72,600 恢复)**。③门修订预登记(约束未来重跑,不追溯本次):CE 数值线废除改仅记录;主门 = 自由续写 n≥48、parseable ≥60% 且拒因以 MEASURE 为主(DYCK≈0);n=48 复评命令已下(追加 32,用精确续训 +50 步实现,独立 --out 不碰正品)。④侧证据入箱:CPU 冒烟与主训并行实测主线 tc 7.9→10.4s(+32%)—— CPU 侧同样抢资源,并行禁令扩展到重 CPU 作业 | 2026-08-05 | reports/PREPRETRAIN_INFRA_AND_SMOKE_2026-08-05.md + PREPRETRAIN_FULL_RESULT_2026-08-05.md(哈希/曲线/拒因谱全档)| EXPERIMENT_PREPRETRAIN 结果节;EXECUTOR 追加 32;梯子 R3=已备货 |

| D93 | **用户令:round-2 就此终止(不跑到 100k)、round-3 从"最好的 pretrain"起步 —— 采纳并落成三步执行单**。①用户理由与规划端评估一致处:剩余 27k 步 ≈ 3 天 GPU,而 D88⑦ 预注册预测本就是"素跑终点 parseable 大概率 <10%";round-3 已定的前提下,终点判读的决策价值已被抽空;round-2 全程评测曲线(至 ~73k,每 1000 步探针+每 5000 步解码腿)在库,历史对照完整,科学损失≈0。**D88⑦ 预测按"用户裁决弃考"存档,不判对错**(诚实记账,非移动球门)。②概念映射入档(用户的 LLM 类比是对的):encoder 真 pretrain = canary 85K 小时(继承,一直是对的半边);decoder pretrain = D91/D92 语法预训练;主训练 = 任务训练。round-2 的病 = decoder 没有合适 pretrain 就进了任务训练。③"最好的 pretrain"的界定(防无限追完美):预训练的作用是装偏置非练满分;强化一档 = 语料 200k→400k、步数 20k→40k、门升 n=48 自由续写(预登记:parseable ≥29/48 且 DYCK≈0);**过门用 v2 工件,不过门贴回并用已合格的 v1 工件照常启动 —— 只强化一次,不追"完美"**。④round-3 配方钉板(单变量纪律):= round-2 配方(池 v3/C1a/音高×2.5/clip 25/lr_dec 3e-4/双节奏评测)+ 唯一变量 --decoder-init(encoder 仍 canary 热启;1c 遮上文继续关,留作后续独立门);max_steps 100k 只定调度形状,**判决不等终点**:预登记 20k 判决点 —— a) 解码腿 DYCK 拒因数 ≤ round-2@20k 同口径一半 b) raw_ned ≤ 0.85 c) 探针 sem 劣化 ≤5% vs round-2@20k;两胜续跑,全败停训贴回(round-3 不是又一场盲跑马拉松)。⑤执行顺序:停主线+归档 ckpt_r2_73k → 语料 v2(CPU ~17 分)→ 预训练 v2(GPU ~1.6h,含 n=48 门)→ round-3 启动;追加 32 的暂停窗清单(1c 门/eval48)被本单取代吸收 | 2026-08-05 | 用户原话;D88⑦/D92;round-2 autolog 全程曲线 | EXECUTOR 追加 33;梯子/预训练卡更新 |

| D94 | **D93 转场三提交验收(v2 按门拒收、round-3 已启动)+ 规划端门设计三宗自评 + 最优旁存补丁**。①追认:a) 对照基线在 round-3 出数前冻结(ROUND3_20K_BASELINE:DYCK ≤21/48、raw_ned ≤0.85、探针宏均 ≥0.562875,同四探针同宏口径防偷换),**我未指定的"仅 1/3 过线"分支被执行端补为"暂停贴回不自动续也不自动判死" —— 收编为正典**;b) v2 强化预训练 40k 步跑完(avg50 1.888,末次 n=48:36/48 可解析 ✓ 但 DYCK=5 ≠0)→ **按预登记门拒收,round-3 用已合格 v1** —— 门纪律教科书执行;c) round-3 启动核验全过:归档 r2_73k(hash 入档)、独立 ckpt_r3_v1 目录(执行端主动加 --ckpt-dir,好改动)、"decoder 预训练初始化已载入"回显、无恢复行、遮上文/音频依赖(关);首步 loss 204/dec gn 340 = 全新 decoder 遇 clip 25,warmup 期被强裁属预期,不是异常。②规划端门设计三宗自评:a) "DYCK≈0"写法含糊,执行端按 =0 落地(可辩护,记档不追改);b) **不对称证据缺陷**:v1 只有 n=4 读数、v2 有 n=48,"v2 拒→用 v1"隐含 v1≥v2 未经同尺检验 —— round-3 已在跑,不折腾换弹,但此缺陷入教训簿:**门必须比"同一把尺上的两个候选",不是比"候选与阈值"**;c) **只存末态**害我们丢了 v2 的 30k 态(45/48・DYCK2,轨迹最优)—— 已补:free_eval_rank=(可解析数,−DYCK) 选优旁存 *_best.pt(跨恢复保序、meta 自带健康门、装载端兼容),LEGATO 式按自由生成选 ckpt 从此用在自己身上(将来 v3 预训练自动受益);tests_pretrain_decoder 8/8。③证据链提醒:round-3 的 eval 自动落盘在 **outputs/ckpt_r3_v1/eval_autolog.md(repo 外,隔离设计)** —— 5k/10k/20k 判读点必须整文件拷入 repo 提交(追加 34 给了命令),否则委员会材料断链。④round-3 时间预估:20k 步 × ~9s ≈ 2.1 天到判决点 | 2026-08-05 | reports/D93_PRETRAIN_V2_AND_ROUND3_START + ROUND3_20K_BASELINE + 3928e3b diff 全审;v2 轨迹表(2k-40k) | pretrain_decoder best 旁存 + 测试;EXECUTOR 追加 34 |

| D95 | **用户质询采纳:不带疑似次优 init 空等 —— 3 小时弹药重整窗 + 门修订(公开自证,非纯先验)**。①定性纠正:v2 预训练**不是失败** —— 可解析线已过(36/48),栽在规划端定得过死的"DYCK=0 绝对零"上;其 30k 最优态(45/48・DYCK2,全项目最好)被"只存末态"丢失(D94 已修旁存)。②决策:round-3 仅数小时,沉没成本≈0 → 停机重整:a) 重跑 v2 配置预训练(同语料 v2/40k 步,新名 v3 防覆盖证据,best 旁存自动生效,~2h);b) v1 补同尺读数(精确续训 +50 步、--free-eval-n 48,~5 分钟,产物即弃只留读数);c) **四候选同尺选优**:{v1@n48, v2-final(36/48 已有), v3-best, v3-final} 按 free_eval_rank=(可解析,−DYCK) 取最大,底线可解析 ≥29/48(全不过线停机待议);入选者 = round-3 重启 --decoder-init。③门修订入档(诚实条款):"DYCK=0"废除,降级为选优第二键;底线仅保留可解析 ≥29/48。理由:n=48 随机评测无法认证绝对零;round-2 病灶是 DYCK 43/48,数量级差距才是信号,个位残留归正训去磨。**披露:本修订受已见 v2 数据启发,非纯先验;但选优对象(v3 轨迹、v1@n48)均未出生,预登记先于它们**。"只强化一次"承诺不受违反:重跑是找回同一实验被存档 bug 丢失的状态,不是加码。④round-3 重启:配方不变、新目录 ckpt_r3_v2、20k 三判据与冻结基线(ROUND3_20K_BASELINE)原样适用(它对照的是 round-2 曲线,与 init 候选无关)。判决点等待(~2 天)是任务训练本身的时间,无捷径,已从 100k 压缩至 20k | 2026-08-05 | 用户质询原话;D93/D94 报告数字;v2 轨迹表 | EXECUTOR 追加 35;EXPERIMENT_PREPRETRAIN 门修订注记 |

## 待用户拍板(OPEN)
| # | 问题 | 背景 |
|---|------|------|
| O4 | 【已裁决 → D38→D44 终局】0.22 试验未达标,回 0.30(D2 纸面) | 全程见 EXPERIMENT_O4_MIX.md;此条关闭 |
| O6 | 【已裁决 → D61】用户方案:68k 续训试验,硬顶 +10k,不行转全新热启 | 预注册见 ROUND2_DATA O6 节;此条关闭 |
| O8 | 【已裁决 → D61】A-训中进池;三闸(磁盘 120+GB/VN-unknown/train-only)过后渲染另发口令 | PDMX_DEDUP_AUDIT.md;此条关闭 |
| O7 | 【不阻塞】16 音色商业 VST 与 DBD 方言要不要上(论文 1/1 复现的最后两块) | 二轮跑起来、仪表说话后再议 |
| O5 | 【已裁决 → D50】M 档 12,000 曲,2026-07-22 用户批准;备料即刻开始,进池仍等 C2 判决 | 见 D50;此条关闭 |
