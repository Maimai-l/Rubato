# EXPERIMENT_SPEED —— 训练提速(v2:进程预取;线程版已召回)

## v1(线程)判决:有害,召回(2026-07-20,D40)

用户实测线程版:**GPU 利用率 ~50% → ~20%,显存占用同降** —— 比不修还慢。机制:装批线程
与主训练线程共享同一解释器的 GIL 和 CPU 线程池,主线程发射 GPU kernel 的节奏被装批线程
卡死。这是规划端设计失误(官方 DataLoader 用进程不用线程,正为此理)。线程路径已整体
移除,不留旗子。

## v2(进程)设计:同一生成器搬进子进程 + 全故障自动兜底

- 子进程跑【同一个】train_batches 生成器,批按值序列化(显式 pickle 成字节;张量默认的
  共享内存引用会随子进程死亡失效,实测过)经进程队列传回;主进程只喂 GPU。两进程各持
  解释器,零 GIL 交互。批内容/顺序与串行逐字节相同(tests_prefetch 真类端到端钉死)。
- **27h 无人值守兜底链**(任一故障 → 打「预取:」开头的现场行 → 自动退回串行装批,绝不停训):
  ① 启动失败(不可 pickle)→ 当场串行;② 子进程静默死亡 → 秒级发现(不傻等超时);
  ③ 子进程异常 → 完整栈带回;④ 卡死超时(首批 1800s/后续 600s)→ 杀之退串行;
  ⑤ 主侧接收/反序列化异常 → 同退。退回后本 epoch 从头重放,已耗前 n 批重复训练(无害,行内记 n)。
  **最坏情况 = 修复前的串行速度,27h 也够跑过 61000 判决点(9.2k 步)。**
- 子进程强制 CUDA_VISIBLE_DEVICES=""(纯 CPU,不抢显存);SentencePieceProcessor 的
  pickle 往返已实测(编码结果一致);spawn 语义与执行端 Windows 相同,沙盒测的就是 spawn。

## 证据链(2026-07-20)

1. **用户实测**(nvidia-smi -l 5,训练进行中):GPU 利用率 43-72% 波动,均值 ~55%;
   CPU ~70%;VRAM 15,913-15,924 MiB(≈占满 16GB)+ 少量共享 GPU 内存;主内存 ~50%。
2. **代码事实**(dataset.py:389-394):train_batches 是纯串行生成器——主进程装一批
   (soundfile 解码 + soxr 重采样 + SentencePiece 编码)→ GPU 算一批 → 再装下一批。
   装批与算批零重叠,GPU 每批都停等 CPU。与 ① 的波动形态吻合。
3. **规划端前判修正**:此前"≈190×实时 ≈ 接近算力带宽,配置无罪"系估算,被实测推翻
   (真算力带宽 ≈ 190×/0.55 ≈ 350×实时,近半在空转)。估算≠测量,记 D39。

## 修法(已实现,tests_prefetch.py 六项钉死)

`train.prefetch_batches(gen, depth)`:后台线程跑【同一个】串行生成器,提前装批入有界
队列(默认深 3),GPU 算当前批时 CPU 并行装下一批。
**语义保证**:同一生成器、同一调用顺序,批内容/顺序/RNG 消耗与直迭代逐字节相同——
只改"何时装",不改"装什么"。故 O4/61000 步进制判据、resume 语义(tests_resume 复验绿)
全部不受影响。`--prefetch 0` 一个旗子退回串行(A/B 对照/紧急回滚)。

## 预登记判据(v2 沿用 v1 的线,不因返工而放宽)

- 基线:**10.5-10.6 秒/步**(出处:autolog eval 时间戳,41000→44000 = 8h47m/3000 步;
  50000→53000 = 8h51m/3000 步)。
- 测量:重启后相邻 eval 时间戳差 ÷1000 = 秒/步(用户 27h 后回来,期间落 9-15 个 eval,
  判速度的同时正好覆盖 61000 的 O4 判决点,一次复盘判两案)。
- **成功:秒/步 ≤ 9.0(提速 ≥15%)**;假设完全成立的预期区 = 6.5-8.0 秒/步,GPU ≥80%。
  27h 折算:成功区 ≈ 12-15k 步(到 ~65-68k);最坏(全程回退串行)≈ 9.2k 步(到 ~62k),
  两种情况都过 61000。
- **不足:提速 <10%** 且日志无「预取:」回退行 → 单产者进程喂不饱,v3 = 产者内并行读音频
  (需先审采样 RNG 顺序,另立判据);若有回退行 → 按行内现场分诊,先修故障再谈提速。
- 回滚:`--prefetch 0` 一旗回到纯串行(与 D39 之前行为全同)。

## 挂账:VRAM 贴顶 + 共享内存(另一个独立嫌疑)

15.9/16 GB + "用一点共享内存":Windows 驱动在显存不足时会**静默**把分配溢出到系统内存
(WDDM sysmem fallback),不报 OOM 只变慢。待执行端贴任务管理器"共享 GPU 内存"具体数:
持续 >0.5 GB → 下一杆 = `--max-batch-sec 50`(学习语义中性:累积目标 2000s 不变,
micro-batch 变小变多,腾 1-2GB 余量)。本轮不动,一次只动一个变量。

## 答执行端 SPEED_CONCERN(@2bb6143,两问两答 + 两个数字要核)

**问1:accum 2000→1000 有无质量风险?—— 有,且换不来学习提速,不做。**
步数不是学习量,**流过模型的音频秒数才是**:降 accum 只是把同样的 fwd+bwd 流水
切成两倍多的"步",每步学一半的数据,GPU 每小时处理的音频秒数一秒不多;
副作用全是真的——有效批减半(梯度噪声↑)、cosine 调度位置/61000 判据/54k 步全部
历史基线的"步"定义作废。你的 66 秒/步推算与实测不符:eval 时间戳铁账 10.5-10.6 秒/步
(41000→44000 = 8h47m/3000 步),即每次 fwd+bwd ≈0.2-0.3s,不是你假设的 ~2s。

**问2:gradient checkpointing / 减层?—— checkpointing 进备选库,减层否决。**
减 conformer 层数 = 毁 canary 热启动,不议。checkpointing 是"用 +30% 计算换激活内存",
只有在溢出坐实、且 --max-batch-sec 50 压不回去时才净赚,顺位在后。

**要核的两个数字(你的报告缺出处,先补证据再定案):**
1. "PyTorch 分配 28-29GB / 13GB 走系统 RAM"——出处是什么?任务管理器"共享 GPU 内存"
   面板显示的 **16GB 是容量(=主内存一半),不是占用**,极易误读。
   请贴:任务管理器 → 详细信息 → 右键列 → 添加"专用 GPU 内存"+"共享 GPU 内存"→
   **python.exe 那一行的两个具体数**。共享列 ≥ 数 GB = 溢出坐实(升级为头号瓶颈,
   杆:--max-batch-sec 50);≤ 几百 MB = 误读,结案。
2. "50 步 20-30 分钟"——**用户直接否定("这是他的幻觉"),结案,不再跟查**。
   无日志出处的目测速度一律不进账;速度的唯一记账口径 = autolog 相邻 eval 时间戳
   (这也是预取判决本来就要用的数据,不受此条影响)。

## 执行(命令与上次完全相同,prefetch=3 是缺省)

停训 → `git pull --rebase --autostash` → 原命令重启(--clip-norm 25 --lr-dec 3e-4
--amt-mix 0.22)。贴回(新文件 reports/SPEED_RESTART.txt):
① 配置回显行(应见 `prefetch=3`);② `续训:恢复 step=…` 行;
③ 重启 ≥30 分钟后的 nvidia-smi 采样(≥10 行)+ 任务管理器"共享 GPU 内存"数值;
④ 此后逢 eval 照常 push autolog(时间戳就是判决数据,无需额外操作)。
