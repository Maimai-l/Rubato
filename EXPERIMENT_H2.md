# EXPERIMENT_H2 —— 降低 decoder 学习率(5e-4 → 3e-4)

> 给执行端:这张卡只做一件事,把 decoder 学习率降到 3e-4 继续训,跑 2000 步以上,把日志贴回。

## 为什么(数据出处:用户贴回日志 8500-12700,2026-07-14/15)

step 12000 复盘结果:训练在明显减速。
- A2S 从 3.15(8500)到 3.02(12700),折算每 500 步只降 0.017 —— 之前是 0.11,慢了 6 倍。
- AMT 干脆不动了(2.51 → 2.52,4200 步零进展)。
- 照现在的速度,A2S 降到 2.8 还要 6000+ 步,到 2.5 要 15000+ 步,而且还在继续变慢。

这满足了 EXPERIMENT_H1 §7 事先定下的升级条件(速度慢于 -0.06/500 且 A2S>2.8)。

为什么选"降 decoder lr"这一招:loss 卡在一个水平不再下降、而梯度并不小,常见原因是
学习率太高 —— 每步跳得太大,在谷底附近来回弹,下不去。三个参照项目(LEGATO/M2ST/Canary)
的峰值 lr 都是 3e-4,我们 decoder 是 5e-4,偏高;我们的 batch 又比别人小得多,噪声更大。
详见 REF_EXTERNAL_RECIPES.md。encoder 这次不动(它拿到的梯度和 decoder 一样大,不缺信号)。

## 怎么跑

```bat
:: (0) 拉最新(这次更新包含 --lr-dec 参数;不拉新代码,--lr-dec 会直接报错)
git pull --rebase --autostash

:: (1) 停掉当前训练(Ctrl+C 或按老规矩杀该 python 进程),备份快照
copy /Y D:\vscode_projects\ee_download\outputs\ckpt\last.pt D:\vscode_projects\ee_download\outputs\ckpt\last_pre_h2.pt

:: (2) 用新学习率续训(clip 保持 25 不变)
python scripts/build_dataset.py --clip-norm 25 --lr-dec 3e-4
```

启动后核对两行(都贴回来):
- 第一行`训练配置回显: ... lr_dec=3.0e-04 ...`
- 续训行`lr 按当前配置重刷 enc=... dec=2.9e-04 左右`
  (代码已保证 CLI 的 lr 能穿透快照;快照默认会把 lr 还原成旧值,这次更新专门修了这一点,
  没有这两行的确认,实验不算开始。)

跑 **2000 步以上**(约到 step 14700+),中途的 eval 正常跑。

## 判定标准(现在写死,到时候按这个执行)

对比窗口:重启点到 +2000 步,看 A2S 的 200 窗均值变化。
- 降幅 ≥ 0.14(比现在的速度快一倍以上)→ 有效,3e-4 保持,继续长跑。
- 降幅 ≤ 0.07(和现在没区别)→ 无效。lr 留在 3e-4(反正无害),问题另找 ——
  下一步是查"为什么 eval 永远只吐 '|4/4k0'"(教师强制探针,规划端出工具)。
- 两者之间 → 再跑 2000 步。
- 参考信号:AMT 若从横盘恢复下降,是"lr 太高"成立的加分证据。

## 止损(不变)

日志出现 nan,或 avg50 连续两个打印行超过 90 → 停,恢复备份
(`copy /Y ...last_pre_h2.pt ...last.pt`),原参数重启,贴日志。

## 贴回清单

1. 配置回显行 + 续训行(见上,确认 lr 真的换了)。
2. 重启点到 +2000 步的全部 step 日志行。
3. 途中每次 eval 的输出块(样例预测那几行尤其要)。
