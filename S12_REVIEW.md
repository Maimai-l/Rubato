# S12 infer.py 执行端实现审查

执行者自发实现了 S12。以下是我的审查判断。

## 审查的前提限制(必须说清)

**我审查的是执行者的自述摘要,不是实际代码文件**——infer.py 在本地 Windows,我在沙盒看不到。
自述摘要恰恰是"可能与实际不符"的东西。所以下面的判断分两类:
- 对摘要所述**设计决策**的评估(我能判断)。
- 需要**看实际代码或真实解码输出**才能定论的(我标注出来,执行者需自验)。

## ✅ 设计正确的部分(与 SPEC 相符)

- **TAST prompt**:`["<|sot|>","<|score|>","<|spell|>","<|ts|>","<|nomidi|>"]` 与 R-S12.1 及
  build.py 的 `DIALECT_PROMPT["TAST"]` 一致。✓
- **窗口/步长**:40s 窗、20s hop = R-S12.1.1。✓
- **>20s = EOS**:`_truncate_after_20s` 阈值 bin=2000 = R-S12.1.2 且与 4000-bin/10ms 编码一致
  (2000×10ms=20s)。✓
- **beam=4 + greedy 重试**:= R-S12.2。✓
- **短音频单窗**:≤40s 直接单窗 = R-S12.3。✓
- **stub 行为**:model=None 或失败返回 `|4/4k0`,过 validate,eval hook 可立即跑 parseable=1.0。
  这是合理的防御设计——让 train.py 的 eval hook 不因 S12 未完成而崩。✓

## 🔴 需要重点核对:merge 算法偏离了 SPEC 的方法

**摘要说**:`merge_windows` 用 "atom-level suffix/prefix longest-match 去重重叠"。
**SPEC R-S12.1.3 说**:"每窗保留完整落在前20s的小节;窗间【在小节线合并】;跨小节延音靠
Dyck open 状态跨窗延续"。

**这是两种不同的算法**,而且我审查时确认字符串匹配法有真实风险:

关键事实(我用合法 A2S 验证过):**A2S 中音符的 offset 标记挂在【下一个小节线】上**
(如 C4 起于小节0,其 offset `c4` 出现在小节1的小节线 `|4/4k0c4`)。因此同一个重叠小节,
在窗1末尾呈现为 offset 形态、在窗2开头呈现为 onset 形态,**两段文本本就不同**。
→ 在 token 流上做 suffix/prefix 最长匹配,重叠区【无法逐字节对上】,会:
  - 匹配过短:重叠没去干净 → 小节重复;
  - 匹配过长:误删非重叠内容 → 丢小节;
  - 或在小节中间断开 → 半个小节。

**好消息**:这些错误都会被 `validate_units` 的 `MEASURE_SUM` / `DYCK` 检查抓住,**不会静默出错**。
**坏消息**:会大量触发 R-S12.2 的"标废+占位小节+n_fail",让推理质量【虚低】——模型可能没那么差,
是合并把好输出judge成了坏输出。A-S12.1 的"重叠区小节一致率>95%"很可能过不了。

**我提供了参考实现** `rubato/model/merge_ref.py`(12 项测试全过),走【IR 语义层】而非字符串:
每窗 A2S → 局部 ScoreIR → 按小节音符签名识别重叠 → 在小节边界拼接 → 投影回 A2S。
Dyck 跨缝闭合由 ScoreIR 结构天然保证。测试证明:两窗重叠合并回精确完整曲、三窗链式、
2小节重叠、坏窗跳过、调号保留全部正确。

**建议**:执行者拿 merge_ref.merge_windows_ref 对照自己的字符串版。若二者在真实解码输出上
结果一致,字符串版可留;若不一致(大概率),换成 IR 层版本。这是 A-S12.1 能否达标的关键。

## 🟡 需要执行者自验的点(我看不到代码)

1. **merge 在剥戳前还是后?** 摘要不明确。若在剥戳【前】做字符串匹配,时间戳 token 会让重叠区
   更加对不上(窗1的小节在其内部 t≈20s,窗2的同一小节 t≈0s,戳完全不同)。必须先剥戳再合并——
   或者干脆走 IR 层(解析时时间戳自然分离)。
2. **`_ensure_terminal_bar` 的拍号从哪来?** 终止小节线需要拍号/调号(如 `|3/4k-2` 不是 `|4/4k0`)。
   若硬编码 4/4k0,非 4/4 曲子的终止线会与前文拍号矛盾 → validate 可能不报(终止线不参与求和)
   但产出 MusicXML 会有错误的尾小节。merge_ref 从首小节继承拍号,可参照。
3. **544 行的体量**:比预期大不少。三种 fallback API 形态(transcribe/generate/direct call)是
   合理的鲁棒设计,但也可能藏边界 bug。建议执行者跑一遍单元测试覆盖 split_audio/strip_timestamps/
   merge 三个纯函数(这些不需 GPU)。

## 结论

**方向对,stub 设计好,大部分 SPEC 点合规——可以让 train.py 的 eval hook 先跑起来。**
但 merge 算法偏离了 SPEC 的小节线方法,用字符串匹配在 A2S 的 offset-挂靠-下一小节线 特性下
有真实风险,很可能让 A-S12.1 达标困难、推理质量虚低。我给了 IR 层参考实现 + 12 测试,
执行者应对照替换。这不阻塞训练启动(stub 保底),但阻塞"推理质量可信"这个目标。
