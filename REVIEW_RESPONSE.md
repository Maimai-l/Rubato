# 训练就绪报告审查响应(1783437775657)

对本地训练就绪报告的逐项审查。总体:**数据管线在真实数据上跑通,结果正确,可以进入训练前三步。**
但报告对风险1的定性有误,已修复;另标注两个次要项。

## ✅ 确认无误(报告是对的)

- **S3 PDMX**:130,275 幸存正确。过滤逻辑按设计工作。`is_transcription + license` 严格过滤归零
  是预期的(见风险3),宽松模式保留 52.1% 是合理的。
- **S6 MAESTRO**:1276/1276 转换,10.4GB,199.23h。流式转 FLAC 完全正确,零失败。
- **S7 nASAP**:446/519 成功,7660 段。失败构成清晰(46 timemap + 25 缺对齐 + 2 未知)。
- **泄漏检测**:抓到 5 个真实 PDMX↔MAESTRO-test 泄漏,黑名单 146 条。这是评测可信度的关键,做对了。
- **三步阻塞排序**(训 tokenizer → 建模型 → S12 stub → 启动训练)正确且依赖顺序无误。

我写的 S3/S6/S7 代码在真实数据上的表现验证了核心判断:**结构正确 + 断言到位的代码,本地接真实数据即用**。

## 🔴 风险1 定性错误 → 已修复(代码改动)

**报告说**:xml_id 匹配率 0.37%,"非阻塞,但应关注"。
**实际**:这个定性低估了问题。0.37% 匹配 → 平均每首曲子仅 8 个锚点 → 7660 段平均每段 0.46 个锚点。
这意味着**大部分段落的 TAST 时间戳是长距离外插,不是真实对齐**——看似合理,实则失真。

**但它确实不阻塞训练**,原因报告没说清:时间戳只影响 TAST dialect。A2S/A2S_lite(nASAP 的
18.8M 字符乐谱监督,即它的主要价值)完全不含时间戳,不受影响。

**根因在我的代码**:`build_xmlid_map` 留了句"执行者需适配 partitura note.id 与 nASAP xml_id 的
对应"——这个适配没做对(partitura 的 note.id 格式与 nASAP 的 "n{measure}-{idx}" 不一致),
导致 join 基本失败。

**修复**:不是去修匹配率(那需要执行者搞清两种 id 的真实对应),而是加**覆盖率门控**——
这正是 SPEC R-S7.2 早已规定的:"对齐置信度低的音符,时间戳略去,该音仍在 A2S"。
- `segment.timemap_covers_segment()`:段内锚点密度 < 阈值(默认 0.5/小节)→ 判定覆盖不足。
- `make_labels()` 改为**分级失败**:A2S/A2S_lite 违规→废弃整条(硬失败);
  TAST 覆盖不足或投影失败→**仅丢 TAST,保留 A2S/A2S_lite**(软失败)。
- `process_nasap_piece()` 传入 `seg_start_whole` 启用门控,统计 `tast_dropped_sparse`。
- 8 项新测试(tests_segment [9]-[12]):稀疏降级、密集正常、硬失败废弃全验证。

**效果**:nASAP 仍贡献全部乐谱监督;失真时间戳不再污染训练。执行者仍可另行修 xml_id 映射
以恢复 TAST 覆盖,但即使不修,训练也不会被脏时间戳带偏。这比"关注它"稳妥得多。

## 🟡 次要项(标注,非阻塞)

**PDMX 下采样**:130,275 超目标池(12-20K)6.5 倍。报告已注意到。建议按 R-S3.4:
work_key 去重后随机截取到 ~15K。`pdmx.cluster_duplicates` 已能做 work_key 去重,执行者接上即可。
注意:下采样必须在黑名单过滤**之后**做,否则可能把该排除的曲留下。

**风险3 license 全空**:PDMX license 字段 100% 空。SPEC R-S3.2 已授权"训练可放宽",所以
当前跳过 license 检查用于训练是**合规的**。但报告的提醒对:若日后要再分发数据/模型,
需从 PDMX Zenodo 文档确认真实许可。这是分发时的事,不是训练阻塞。

## 剩余(与报告一致)

- S12 infer.py:train.py 的 eval hook 依赖 `infer_a2s`。报告说"实现签名能返回合法 A2S 即可,
  正确性首个 checkpoint 后验证"——同意。我可现在写成带 TAST 分窗剥戳结构的完整版。
- S13 evaluate.py:非训练阻塞(训练中不调),但垂直切片评测需要。复用 LEGATO OMR-NED。

## 一句话结论

报告质量高,数据管线真实可用。风险1 我已从"外插失真"改为"门控丢弃",符合 SPEC 原意,
且不阻塞训练。执行者可按三步启动;S12/S13 我可继续写成带断言的结构。
