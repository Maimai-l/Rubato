并行工作建议 — D56/D57 校准冒烟完成后
===========================================

当前状态
--------
* D53 二轮池 v1 已钉板：584,106 utterances；训练仍按红线未启动。
* D56/D57 M2ST 校准冒烟通过：rec1/rec2/rec3 三个 MIDI 均已生成 MusicXML。
* 当前没有已下发的全量评分或训练启动口令。

建议由规划端下发的并行工作
------------------------------
1. 全量 M2ST/LEGATO 评分（主线）
   - 以 D56 冒烟通过为前置，使用同一隔离 venv 与固定版本。
   - 资源：CPU、磁盘；不触碰训练环境。

2. PDMX S3 去重的全面内容审计（独立审计线）
   - 复核 `subset:deduplicated` 被排除的 151,439 条是否为真正音乐内容重复，区分：
     完全一致、近重复、不同编配/错误配对。
   - 只读扫描、输出可复核样本及汇总；不得在审计阶段改 S3 过滤或 manifest。
   - 资源：CPU、磁盘；可与全量评分同时运行。

3. 音频—标签一致性 QC（独立数据 QC 线）
   - 对已入池的 PDMX/nASAP/MAESTRO 条目抽样或全量检查：音频可读、时长、标签方言、
     split 与泄漏隔离状态。
   - 只读输出报告；不改标签或音频。
   - 资源：CPU、磁盘；可与全量评分并行。

资源与安全边界
--------------
* 上述三项均不启动第二轮训练，也不改生产 Python 环境。
* 评分和审计均会高频读盘；若实际启动，建议评分单独占一个工作进程，两个审计任务的
  并发数保持低，以免同时扫描大批小文件造成磁盘争抢。
* 只有收到明确书面任务后才启动第 1–3 项；本文件是建议，不是执行授权。
