# 训练监控状态板(规划端维护;历史见 git log 本文件)

**更新:2026-07-14(继任规划端接手)**

## 当前状态 —— 训练中,悬案 = 平台期

| 指标 | 值 | 出处 |
|------|-----|------|
| step | ≈7600 | 用户粘贴日志 2026-07-14(原文存 HANDOFF.md §4) |
| loss(avg50) | ≈62 | 同上 |
| sem | ≈2.9-3.2 | 同上 |
| 分方言 sem | A2S 3.21 / A2S_lite 3.38 / AMT 2.53 / TAST 2.70 | 同上 |
| gn(裁剪前) | 21.6~37.9,avg50 23.5~27.8(clip=1.0) | 同上,原文照录 HANDOFF §4 |
| parseable | 0.00 @ step 5000/6000/7000(宽限放行) | reports/SMOKE_RESULT.md |
| 样例预测 | 已从空谱兜底进步到合法 A2S 格式前缀 | reports/SMOKE_RESULT.md 末节 |

## H1 已判决:不成立(2026-07-14,满窗 8050-8500)【出处:用户贴回日志+命令原文】

放开裁剪(clip 1.0→25,命令确认生效)后 A2S 斜率 -0.113/500 步,与基线 -0.086/500
基本相同 → 平台期与梯度裁剪无关。enc/dec 比值 0.79~1.32 同量级 → encoder 不缺信号。
处置:clip 留在 25,转 **H3 观察路线**(EXPERIMENT_H1 §7):继续跑、逐次贴 eval、
**step 12000 复盘**;升级 H2(dec lr→3e-4)的条件已提前声明。决策记录:D21。

## 探针首批数据:EOS 论死亡,前缀acc=0.38 中间带,格式/时间戳已学会(2026-07-15 深夜)

`acc=0.40 前缀acc=0.38 eotP=0.0001`【出处:PROBE_RESULT.txt @ccbcc72】。判读:
① eotP 万分之一 → "开口就想收工"排除;② 0.38 在预登记中间带(0.30-0.60),执行端
"decoding bias confirmed" 不获采纳,需第二个 eval 数据点;③ argmax/参照对照显示格式骨架
与时间戳量级已学会、音高调号未准 —— "学到一半"面貌;④ 报告是 eval 中途推的,缺 解码现场/汇总。
**上报改革**:eval 证据由代码自动落盘 reports/eval_autolog.md,执行端只 commit(第三次
改写旧文件后收权)。等下一次 eval 的 autolog。

## 重大修正:'|4/4k0' 是兜底常量,不是模型输出(2026-07-15 深夜)【出处:PROBE_RESULT.txt + infer.py:24】

本轮 eval `empty=1.0`:48 样本全走了 `_EMPTY_A2S` 兜底 → 既往"模型只会输出开头"的
解读全部作废,推理管线三处静默吞错才是真相。已修:探针 device bug;已加:吞错现场记录
(validate 拒绝时打印模型真实原始输出)。等下一轮 eval 的 `探针acc` + `解码现场` 行,
就能第一次看到模型真正在生成什么。训练本身健康推进:step 16600,A2S 2.93(新低),AMT 2.40。
执行端第二次改写报告文件(5c48581 删 49 行),规矩升级:只写新编号文件。当前卡:EXPERIMENT_PROBE.md 第二版。

## H2 判决:无效;转探针路线(2026-07-15 晚)【出处:reports/H2_steps.txt】

降 decoder lr(5e-4→3e-4)满窗 2000 步:锚点 13000 A2S=3.02 → 14900 仍 3.02,一分没降
(14500 的 2.95 弹回,波动)。优化器侧三嫌疑(裁剪/encoder 信号/decoder lr)**全部实验排除**。
当前任务 = **EXPERIMENT_PROBE.md**:eval 内置教师强制探针,分诊"生成停在开头"是
模型未熟(病 A)还是暴露偏置/解码问题(病 B),判读阈值已预声明(前缀acc 0.6/0.3)。
事故记录:执行端 cd996eb 删了报告的 eval 段,已从 git 历史恢复;报告规矩(只增不删)已立。

## step 12000 复盘:减速 6 倍,升级条件触发 → 开 H2(2026-07-15)【出处:贴回 8500-12700】

A2S 斜率从 -0.11/500(8050-8500)掉到 **-0.017/500**(8500-12700);AMT 4200 步零进展
(2.51→2.52);eval 样例预测连续 4 次原样 '|4/4k0',parseable 0.00-0.02。
触发 D21 预声明的升级条件 → **当前任务 = EXPERIMENT_H2.md**:`--lr-dec 3e-4` 跑 2000 步,
判据在卡上写死。配套:lr CLI + 快照穿透修复(apply_cfg_lrs)。决策记录:D22。
若 H2 无效,下一步已排队:教师强制探针,查"为什么生成永远停在开头"。

## 当前动作

**EXPERIMENT_H1.md** 对照实验已下发(`--clip-norm 25` × ≥500 步,预登记判据见卡/D20),
等执行端贴回。贴回后由规划端按卡判决 H1,并用同批数据的 enc=/dec= 列分流 H2/H3。

外部配方参照已入库:**REF_EXTERNAL_RECIPES.md**(LEGATO / M2ST / Canary 一手配方,
2026-07-14 原文核对)。要点:我们架构的官方配方 `gradient_clip_val: 0.0`(不裁剪)、
`label_smoothing: 0.0`;三家峰值 lr 一致 3e-4;外部步数视野 40k-225k(H3 佐证)。
仅作实验设计先验,判决仍归贴回数据。

## 旧状态更正(2026-07-13 版说"训练最终状态—已暂停"已过时)

- step 4000 的 `parseable_rate=0.00` 暂停:已被双闸策略取代(步数宽限 + sem>2.0 门,
  commit b683a/HANDOFF §3),训练已续至 7600+。
- "等规划端修 infer.py 的 NeMo transcribe 适配":已解决 —— NeMo transcribe 与换表模型
  不兼容,走自研贪心解码 autoregressive_decode(commit af84a7f),快路径带 forward 自校验。
- AMT 超长丢弃 32,930 → 0(重切窗 144k,commit 3a0e82f / d2ba970)。
